const products=[
{id:'MH0000128',name:'男士厚手格纹中筒袜 5双装',category:'0105 男袜',brand:'SOCKONE',owner:'占树全',status:'准备中',skuCount:5,defaultSku:'MH0000128-01',source:'1688',image:'🧦'},
{id:'MH0000129',name:'女士棉混中筒袜',category:'0104 女袜',brand:'SOCKONE',owner:'小于',status:'待确认',skuCount:3,defaultSku:'MH0000129-01',source:'1688',image:'🧦'},
{id:'MH0000130',name:'男士商务中筒袜 5双装',category:'0105 男袜',brand:'SOCKONE',owner:'占树全',status:'可发布',skuCount:4,defaultSku:'MH0000130-01',source:'1688',image:'🧦'}
];
const skus=[
{id:'MH0000128-01',productId:'MH0000128',name:'男士厚手格纹中筒袜 黑色',variant:'黑色 · 24–27cm · 5双装',origin:'中国',jan:'JAN待分配',status:'准备中',image:'🧦'},
{id:'MH0000128-02',productId:'MH0000128',name:'男士厚手格纹中筒袜 蓝色',variant:'蓝色 · 24–27cm · 5双装',origin:'中国',jan:'JAN待分配',status:'准备中',image:'🧦'},
{id:'MH0000128-03',productId:'MH0000128',name:'男士厚手格纹中筒袜 灰色',variant:'灰色 · 24–27cm · 5双装',origin:'中国',jan:'JAN待分配',status:'准备中',image:'🧦'},
{id:'MH0000128-04',productId:'MH0000128',name:'男士厚手格纹中筒袜 棕色',variant:'棕色 · 24–27cm · 5双装',origin:'中国',jan:'JAN待分配',status:'准备中',image:'🧦'},
{id:'MH0000128-05',productId:'MH0000128',name:'男士厚手格纹中筒袜 绿色',variant:'绿色 · 24–27cm · 5双装',origin:'中国',jan:'JAN待分配',status:'准备中',image:'🧦'}
];
const state={tab:'products',view:'cards',cols:Number(localStorage.getItem('aione-product-card-cols')||3)};
const generator={step:1,productId:'MH0000131',name:'男士棉混休闲袜 5双装',category:'0105 男袜',source:'1688 API / 商品机会',brand:'SOCKONE',colors:['黑色','蓝色','灰色','棕色','绿色'],sizes:['24–27cm'],packs:['5双装'],disabled:new Set(),origin:'中国',status:'草稿'};
const $=(q)=>document.querySelector(q), $$=(q)=>Array.from(document.querySelectorAll(q));
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
function status(v){return `<span class="status ${v==='待确认'?'warn':''}">${esc(v)}</span>`}
function actions(id){return `<div class="actions"><button class="icon" title="编辑" data-open="${id}">✎</button><button class="icon" title="评论">☵</button><button class="icon" title="关注">☆</button><button class="icon" title="更多" data-open="${id}">⋮</button></div>`}
function productTable(){return `<section class="table"><div class="thead"><div>商品对象</div><div>SKU</div><div>负责人</div><div>分类</div><div>状态</div><div>对象操作</div></div>${products.map(p=>`<div class="trow"><div class="entity"><div class="thumb">${p.image}</div><div><div class="entity-id">${p.id}</div><div class="entity-name">${esc(p.name)}</div><div class="entity-sub">${p.brand} · ${p.source}</div></div></div><div class="cell"><strong>${p.skuCount} SKU</strong><small>${p.defaultSku}</small></div><div class="cell"><strong>${p.owner}</strong></div><div class="cell"><strong>${p.category}</strong></div><div>${status(p.status)}</div>${actions(p.id)}</div>`).join('')}</section>`}
function productCards(){return `<section class="cards" style="--card-cols:${state.cols}">${products.map(p=>`<article class="card"><div class="card-top"><div class="thumb">${p.image}</div><div class="entity"><div><div class="entity-id">${p.id}</div><div class="entity-name">${esc(p.name)}</div><div class="entity-sub">${p.category} · ${p.brand}</div></div></div></div><div class="card-facts"><div class="fact"><small>SKU</small><strong>${p.skuCount} SKU</strong></div><div class="fact"><small>负责人</small><strong>${p.owner}</strong></div><div class="fact"><small>默认SKU</small><strong>${p.defaultSku}</strong></div></div><div class="card-foot">${status(p.status)}${actions(p.id)}</div></article>`).join('')}</section>`}
function skuTable(){return `<section class="table"><div class="thead" style="grid-template-columns:minmax(320px,1.8fr) 210px 90px 120px 100px 150px"><div>SKU对象</div><div>规格组合</div><div>原产国</div><div>JAN / GTIN</div><div>状态</div><div>对象操作</div></div>${skus.map(s=>`<div class="trow" style="grid-template-columns:minmax(320px,1.8fr) 210px 90px 120px 100px 150px"><div class="entity"><div class="thumb">${s.image}</div><div><div class="entity-id">${s.id}</div><div class="entity-name">${esc(s.name)}</div><div class="entity-sub">所属 Product：${s.productId}</div></div></div><div class="cell"><strong>${s.variant}</strong></div><div class="cell"><strong>${s.origin}</strong></div><div class="cell"><strong>${s.jan}</strong><small>编码中心统一分配</small></div><div>${status(s.status)}</div>${actions(s.id)}</div>`).join('')}</section>`}
function settings(){return `<section class="table"><div style="padding:18px 20px"><h3 style="margin:0 0 8px;font-size:15px">商品中心设置</h3><p style="margin:0 0 16px;color:var(--muted);font-size:11px">商品中心只维护本中心规则。规格模板由规格中心管理，编码由编码中心管理。</p><div class="grid2"><div class="kv"><small>Product编号规则</small><strong>MH + 7位序号</strong></div><div class="kv"><small>SKU编号规则</small><strong>Product ID + 两位子序号</strong></div><div class="kv"><small>默认卡片密度</small><strong>3列</strong></div><div class="kv"><small>正式商品规则</small><strong>正式Product至少1个SKU</strong></div></div></div></section>`}
function syncViewButtons(){
  $('#listBtn').classList.toggle('active',state.view==='list');
  $('#cardBtn').classList.toggle('active',state.view==='cards');
  $('#cardBtn').textContent=`▦ 卡片 ${state.cols}列 ▾`;
  $$('#densityMenu button').forEach(b=>b.classList.toggle('active',Number(b.dataset.cols)===state.cols));
}
function render(){
  $$('.tabs button').forEach(b=>b.classList.toggle('active',b.dataset.tab===state.tab));
  syncViewButtons();
  $('#content').innerHTML=state.tab==='products'?(state.view==='list'?productTable():productCards()):state.tab==='sku'?skuTable():settings();
  $$('[data-open]').forEach(b=>b.onclick=()=>openObject(b.dataset.open));
}
function openObject(id){const p=products.find(x=>x.id===id),s=skus.find(x=>x.id===id);const obj=p||s;if(!obj)return;const isSku=!!s;$('#drawerContent').innerHTML=`<div class="drawer-body"><div class="context-head"><div class="thumb">${obj.image||'🧦'}</div><div><div class="entity-id">${obj.id}</div><div class="entity-name">${esc(obj.name)}</div><div class="entity-sub">${isSku?'SKU对象':'Product对象'}</div></div></div><div class="object-actions"><button>编辑</button><button>负责人</button><button>状态</button><button>评论</button><button>@成员</button><button>关注</button><button>收藏</button><button>附件</button><button>关联对象</button><button>更多</button></div><div class="object-tabs"><button class="active">完整资料</button><button>SKU</button><button>时间线</button><button>操作记录</button></div><div class="panel"><div class="grid2">${isSku?`<div class="kv"><small>所属Product</small><strong>${s.productId}</strong></div><div class="kv"><small>规格组合</small><strong>${s.variant}</strong></div><div class="kv"><small>原产国</small><strong>${s.origin}</strong></div><div class="kv"><small>JAN / GTIN</small><strong>${s.jan}</strong></div>`:`<div class="kv"><small>商品状态</small><strong>${p.status}</strong></div><div class="kv"><small>三级分类</small><strong>${p.category}</strong></div><div class="kv"><small>品牌</small><strong>${p.brand}</strong></div><div class="kv"><small>SKU</small><strong>${p.skuCount} SKU</strong></div><div class="kv"><small>来源</small><strong>${p.source}</strong></div><div class="kv"><small>负责人</small><strong>${p.owner}</strong></div>`}</div></div></div>`;openLayer('drawer');}
function openLayer(type){$('#backdrop').classList.add('open');if(type==='drawer')$('#drawer').classList.add('open');else $('#wizard').classList.add('open')}
function closeLayers(){$('#backdrop').classList.remove('open');$('#drawer').classList.remove('open');$('#wizard').classList.remove('open')}
function combinations(){let n=0;const rows=[];for(const color of generator.colors){for(const size of generator.sizes){for(const pack of generator.packs){n++;const key=`${color}|${size}|${pack}`;rows.push({key,color,size,pack,index:n,enabled:!generator.disabled.has(key),id:`${generator.productId}-${String(n).padStart(2,'0')}`,name:`${generator.name.replace(/\s*5双装$/,'')} ${color}`,origin:generator.origin,status:generator.status,image:`${color}图`,jan:'待分配'});}}}return rows}
function stepper(){return `<div class="sku-stepper"><div class="sku-step ${generator.step>=1?'active':''}"><span>1</span><div><strong>确认商品</strong><small>Product事实</small></div></div><div class="sku-step-line"></div><div class="sku-step ${generator.step>=2?'active':''}"><span>2</span><div><strong>确认规格</strong><small>规格模板与Variant</small></div></div><div class="sku-step-line"></div><div class="sku-step ${generator.step>=3?'active':''}"><span>3</span><div><strong>确认SKU</strong><small>组合、批量、图片与编码状态</small></div></div></div>`}
function sourceBadge(text){return `<span class="source-badge">${text}</span>`}
function renderGenerator(){const rows=combinations(),active=rows.filter(x=>x.enabled);let body='';
 if(generator.step===1){body=`<div class="generator-hero"><div class="product-context"><div class="generator-thumb">🧦</div><div><div class="entity-id">${generator.productId}</div><h2>${generator.name}</h2><p>${generator.category} · ${generator.brand}</p></div></div><div class="source-stack">${sourceBadge('1688 API')}${sourceBadge('商品机会')}${sourceBadge('AI整理')}</div></div><div class="generator-section"><div class="section-title"><div><h3>确认 Product 基本事实</h3><p>先确认商品身份，再进入规格识别。正式 Product 建立时必须同时至少生成 1 个 SKU。</p></div></div><div class="form-grid"><div class="field"><label>Product ID</label><input value="${generator.productId}" readonly></div><div class="field"><label>三级分类</label><input value="${generator.category}" id="gCategory"></div><div class="field"><label>商品简称</label><input value="${generator.name}" id="gName"></div><div class="field"><label>品牌</label><input value="${generator.brand}" id="gBrand"></div><div class="field"><label>来源</label><input value="${generator.source}" readonly></div><div class="field"><label>规格模板</label><input value="男袜标准规格模板" readonly></div></div></div>`}
 if(generator.step===2){body=`<div class="generator-section"><div class="section-title"><div><h3>系统已识别规格维度</h3><p>员工只确认、删除或补充规格值，不逐个创建 SKU。规格模板来自规格中心。</p></div><button class="ghost-btn" id="reDetect">↻ 重新识别</button></div><div class="dimension"><div class="dimension-head"><strong>颜色</strong><div>${sourceBadge('1688 API')}${sourceBadge('AI匹配图片')}</div></div><div class="variant-cards">${generator.colors.map(c=>`<button class="variant-card"><span class="variant-swatch">🧦</span><strong>${c}</strong><small>${c}图 · 已匹配</small></button>`).join('')}<button class="variant-card add">＋ 添加颜色</button></div></div><div class="dimension"><div class="dimension-head"><strong>尺码</strong><div>${sourceBadge('规格模板')}${sourceBadge('API')}</div></div><div class="chips"><span class="chip">24–27cm</span><button class="chip add">＋ 添加</button></div></div><div class="dimension"><div class="dimension-head"><strong>套装数量</strong><div>${sourceBadge('API')}</div></div><div class="chips"><span class="chip">5双装</span><button class="chip add">＋ 添加</button></div></div><div class="combination-summary"><div><small>候选组合</small><strong>${rows.length}</strong></div><div><small>当前启用</small><strong>${active.length}</strong></div><div><small>计算方式</small><strong>${generator.colors.length}颜色 × ${generator.sizes.length}尺码 × ${generator.packs.length}套装</strong></div></div><p class="generator-note">候选SKU不是机械笛卡尔积。优先级：API事实 ＞ 确定性规则 ＞ AI建议 ＞ 人工最终确认。</p></div>`}
 if(generator.step===3){body=`<div class="generator-section"><div class="section-title"><div><h3>确认 ${active.length} 个 SKU 候选</h3><p>关闭不存在的组合，批量设置共同事实，再一次性创建 Product + SKU。</p></div><div class="view-cluster"><button class="active">矩阵</button><button>列表</button></div></div><div class="bulk-bar"><label><input type="checkbox" id="selectAll" checked> 全选</label><select id="bulkOrigin"><option value="中国">原产国：中国</option><option value="日本">原产国：日本</option><option value="">原产国：待确认</option></select><select id="bulkStatus"><option value="草稿">状态：草稿</option><option value="准备中">状态：准备中</option></select><button id="applyBulk">应用到全部启用SKU</button><button>批量关联图片</button></div><div class="sku-matrix"><div class="matrix-head"><div>颜色</div>${generator.sizes.map(s=>`<div>${s}</div>`).join('')}</div>${generator.colors.map(color=>`<div class="matrix-row"><div class="matrix-label"><strong>${color}</strong><small>${color}图</small></div>${generator.sizes.map(size=>{const key=`${color}|${size}|${generator.packs[0]}`,row=rows.find(x=>x.key===key);return `<label class="matrix-cell ${row.enabled?'enabled':''}"><input type="checkbox" data-combo="${row.key}" ${row.enabled?'checked':''}><span>${row.enabled?'✓':'—'}</span><small>${generator.packs[0]}</small></label>`}).join('')}</div>`).join('')}</div><div class="sku-result-head"><strong>SKU生成结果</strong><span>${active.length} 个启用组合</span></div><div class="sku-result-list">${active.map(r=>`<div class="sku-result-row"><div class="sku-image">🧦<small>${r.image}</small></div><div><b>${r.id}</b><strong>${r.name}</strong><small>${r.color} · ${r.size} · ${r.pack}</small></div><div><small>原产国</small><strong>${r.origin}</strong></div><div><small>JAN / GTIN</small><strong>${r.jan}</strong><em>编码中心</em></div><div><small>状态</small><strong>${r.status}</strong></div></div>`).join('')}</div><p class="generator-note">SKU图片优先使用1688 SKU图，其次素材库已有图，再由AI匹配；只有无法确定时才人工修正。JAN / GTIN 生成后交给编码中心处理。</p></div>`}
 $('#wizardBody').innerHTML=`${stepper()}${body}<div class="wizard-actions generator-actions"><button data-close>取消</button>${generator.step>1?'<button id="prevStep">上一步</button>':''}<div class="action-spacer"></div>${generator.step<3?'<button class="primary" id="nextStep">继续</button>':'<button class="primary" id="confirmSku">创建 Product + SKU</button>'}</div>`;
 $$('[data-close]').forEach(b=>b.onclick=closeLayers);if($('#prevStep'))$('#prevStep').onclick=()=>{generator.step--;renderGenerator()};if($('#nextStep'))$('#nextStep').onclick=()=>{if(generator.step===1){generator.category=$('#gCategory').value;generator.name=$('#gName').value;generator.brand=$('#gBrand').value}generator.step++;renderGenerator()};
 $$('[data-combo]').forEach(cb=>cb.onchange=()=>{cb.checked?generator.disabled.delete(cb.dataset.combo):generator.disabled.add(cb.dataset.combo);renderGenerator()});
 if($('#selectAll'))$('#selectAll').onchange=e=>{const all=combinations();if(e.target.checked)generator.disabled.clear();else all.forEach(x=>generator.disabled.add(x.key));renderGenerator()};
 if($('#applyBulk'))$('#applyBulk').onclick=()=>{generator.origin=$('#bulkOrigin').value||'待确认';generator.status=$('#bulkStatus').value;renderGenerator()};
 if($('#confirmSku'))$('#confirmSku').onclick=()=>{const enabled=combinations().filter(x=>x.enabled);alert(`预览确认：${generator.productId} + ${enabled.length} 个SKU已建立。下一事件：成本中心计算单件销售成本。`);closeLayers()};
}
function openWizard(){generator.step=1;generator.disabled.clear();renderGenerator();openLayer('wizard')}
function init(){
  $$('.tabs button').forEach(b=>b.onclick=()=>{state.tab=b.dataset.tab;render()});
  $('#listBtn').onclick=()=>{state.view='list';render()};
  $('#cardBtn').onclick=e=>{e.stopPropagation();state.view='cards';$('#densityMenu').classList.toggle('open');render()};
  $$('#densityMenu button').forEach(b=>b.onclick=e=>{e.stopPropagation();state.cols=Number(b.dataset.cols);localStorage.setItem('aione-product-card-cols',state.cols);state.view='cards';$('#densityMenu').classList.remove('open');render()});
  $('#displayBtn').onclick=e=>{e.stopPropagation();$('#displayMenu').classList.toggle('open')};
  $('#newProduct').onclick=openWizard;
  $('#closeDrawer').onclick=closeLayers;$('#closeWizard').onclick=closeLayers;$('#backdrop').onclick=closeLayers;
  document.addEventListener('click',e=>{if(!e.target.closest('.dropdown'))$$('.menu').forEach(m=>m.classList.remove('open'))});
  render();
}
document.addEventListener('DOMContentLoaded',init);