import { aioneApi } from "../services/aione-api-client.js";
import { getCollaborationData } from "../data/collaboration-store.js";
import { announceWorkItemsChanged, computeWorkAttention } from "../services/work-attention-service.js?v=20260826-v1.9.30.2-work-attention";
import { renderSemanticIcons } from "../config/semantic-icons.js?v=20260824-v1.9.5-sidebar-aside-lock-candidate";
import { MIWA_BUSINESSES } from "../data/miwa-business-home-content.js";
import {
  bindPublicationActions,
  configurePublicationAside,
  publicationBackCover,
  publicationCover,
  publicationCoverSpread,
  publicationPage,
  publicationSpread,
  setPublicationPageMode,
  validatePublicationPages
} from "../components/miwa-publication-master.js?v=20260827-v1.9.31.5";

/* Work Home V2.0 Structure Upgrade | V1.9.33
   原则：后台复杂，前台简单；一份Work Item，多种视图；默认开放可见，敏感例外；看得到 ≠ 要处理 ≠ 能操作。 */
const STATUS_LABELS=Object.freeze({draft:"待整理",pending:"待开始",in_progress:"进行中",active:"进行中",blocked:"有问题",waiting:"待验收",completed:"已完成",done:"已完成",cancelled:"已取消",archived:"已归档"});
const PRIORITY_LABELS=Object.freeze({low:"低",normal:"普通",high:"重要",important:"重要",urgent:"紧急"});
const STATUS_FILTERS=[["all","全部"],["pending","待开始"],["in_progress","进行中"],["blocked","有问题"],["waiting","待验收"],["completed","已完成"]];
const PRIORITY_FILTERS=[["all","全部优先级"],["urgent","紧急"],["high","重要"],["normal","普通"],["low","低"]];
const SORT_OPTIONS=[["ai","美和AI建议优先"],["due","截止时间"],["updated","最近更新"],["priority","优先级"]];
const RELATED_WORK_ENDPOINT="/api/v1/work-home?limit=200"; // V1.9.32兼容：Header/今日工作仍使用相关工作口径。
const ALL_WORK_ENDPOINT="/api/v1/work-home?scope=all&limit=500";
const FOLLOWING_WORK_ENDPOINT="/api/v1/work-home?scope=following&limit=500";
const BUSINESS_BY_ID=Object.freeze(Object.fromEntries(MIWA_BUSINESSES.map((item)=>[item.id,item])));
const BUSINESS_ROUTE_MAP=Object.freeze(Object.fromEntries(MIWA_BUSINESSES.map((item)=>[`work-business-${item.id}`,item.id])));
const CROSSBORDER_WORKBENCHES=new Set(["selection","sampling","procurement","design","publishing","operations","orders","inventory","service"]);
const ROUTE_TITLES=Object.freeze({
  work:"工作概览","work-today":"今日工作","work-all":"全部工作","work-following":"我的关注",
  "work-blocked":"等待与阻塞","work-review":"待验收","work-records":"工作记录",
  "work-mine":"我的工作","work-pending":"待开始","work-active":"进行中","work-waiting":"待验收","work-completed":"工作记录",
  ...Object.fromEntries(MIWA_BUSINESSES.map((item)=>[`work-business-${item.id}`,`${item.name}工作`]))
});

function esc(v=""){return String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));}
function routeId(){return String(window.location.hash||"#/work").replace(/^#\/?/,"").split(/[/?]/)[0]||"work";}
function identity(){return window.AIONEPreviewIdentity||{};}
function dateObject(v){if(!v)return null;const d=new Date(v);return Number.isNaN(d.getTime())?null:d;}
function dateText(v){const d=dateObject(v);return d?d.toLocaleString("zh-CN",{month:"2-digit",day:"2-digit",hour:"2-digit",minute:"2-digit",hour12:false}):"—";}
function dayKey(d=new Date()){return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`;}
function statusLabel(v){return STATUS_LABELS[String(v||"").toLowerCase()]||v||"待确认";}
function priorityLabel(v){return PRIORITY_LABELS[String(v||"").toLowerCase()]||v||"普通";}
function sourceLabel(item){if(item.sourceSystem==="aione-ai-secretary"||item.metadata?.aiProposalId)return "美和AI";if(item.sourceSystem==="aione-web")return "AIONE";return item.sourceSystem||"AIONE";}
function ownerLabel(item){const me=identity();if(String(item.ownerPersonId||"")===String(me.subjectId||""))return me.displayName||"当前用户";return item.ownerName||item.ownerPersonId||"待确认";}
function sourceHref(item){const v=item.metadata?.sourceHash||"";return String(v).startsWith("#/")?v:"";}
function isCompleted(item){return ["completed","done","cancelled","archived"].includes(String(item.status||"").toLowerCase());}
function isOverdue(item){const d=dateObject(item.dueAt);return Boolean(!isCompleted(item)&&d&&d.getTime()<Date.now());}
function isDueToday(item){const d=dateObject(item.dueAt);return Boolean(d&&dayKey(d)===dayKey(new Date()));}
function hasWaitingSignal(item){return Boolean(item.metadata?.waitingReason||item.metadata?.nextReviewAt||item.metadata?.executionState==="waiting");}
function inferBusinessCode(item){
  const explicit=String(item.metadata?.businessCode||item.metadata?.businessId||item.businessId||"").trim();
  if(explicit&&BUSINESS_BY_ID[explicit])return explicit;
  const route=String(item.metadata?.sourceRoute||item.metadata?.sourceHash||item.workbenchCode||"").replace(/^#\/?/,"").split(/[/?]/)[0];
  if(route.startsWith("wholesale-"))return "wholesale";
  if(CROSSBORDER_WORKBENCHES.has(route)||CROSSBORDER_WORKBENCHES.has(String(item.workbenchCode||"")))return "crossborder";
  for(const business of MIWA_BUSINESSES){if(route.includes(business.id))return business.id;}
  return "group";
}
function businessLabel(item){const code=inferBusinessCode(item);return BUSINESS_BY_ID[code]?.name||(code==="group"?"集团 / AIONE":"待归属事业");}
function projectLabel(item){return item.metadata?.projectName||item.metadata?.projectCode||item.projectName||"";}
function teamLabel(item){return item.metadata?.teamName||item.metadata?.teamCode||item.teamName||"";}
function relationLabel(item){
  const me=String(identity().subjectId||"");
  if(String(item.ownerPersonId||"")===me)return "我负责";
  if(String(item.createdByPersonId||"")===me)return "我创建";
  if(item.isFollowing)return "我关注";
  if(item.isParticipant){if(item.participantRole==="reviewer")return "待我确认";return "我参与";}
  return "公司公开";
}
function normalizeRemote(item,personId){
  const owner=String(item.ownerPersonId||""),creator=String(item.createdByPersonId||"");
  return {...item,relation:owner===String(personId)?"mine":creator===String(personId)?"created":item.isFollowing?"following":item.isParticipant?"participant":"shared",status:String(item.status||"pending").toLowerCase(),priority:String(item.priority||"normal").toLowerCase(),sourceSystem:item.sourceSystem||"aione",metadata:item.metadata&&typeof item.metadata==="object"?item.metadata:{},createdAt:item.createdAt||"",updatedAt:item.updatedAt||item.createdAt||"",isFollowing:Boolean(item.isFollowing),isParticipant:Boolean(item.isParticipant),participantRole:item.participantRole||""};
}
function normalizeLocal(t,personId){return {id:t.id,title:t.title,description:t.description||"",goalSummary:t.goalSummary||"",expectedResult:t.expectedResult||"",status:t.status==="done"?"completed":t.status==="active"?"in_progress":String(t.status||"pending"),priority:t.priority||"normal",ownerPersonId:t.assigneeId||personId,createdByPersonId:t.creatorId||personId,workbenchCode:t.workbench||"",relatedObjectId:t.businessObjectId||"",sourceSystem:t.source||"local-preview",createdAt:t.createdAt||"",updatedAt:t.completedAt||t.createdAt||"",dueAt:t.dueDate||"",metadata:{sourceHash:t.route||"",...(t.metadata||{})},relation:String(t.assigneeId||personId)===String(personId)?"mine":String(t.creatorId||"")===String(personId)?"created":"shared",isFollowing:localFollowingIds().has(String(t.id)),isParticipant:false,participantRole:""};}
function localFollowingIds(){try{return new Set(JSON.parse(localStorage.getItem("aione.work.following.v1")||"[]").map(String));}catch(_){return new Set();}}
function saveLocalFollowing(ids){try{localStorage.setItem("aione.work.following.v1",JSON.stringify([...ids]));}catch(_){}}
function todayScore(item){if(isCompleted(item))return -9999;const p={urgent:500,high:360,important:360,normal:220,low:100}[item.priority]||180;const s={blocked:260,waiting:180,in_progress:120,active:120,pending:80,draft:40}[item.status]||40;let due=0;if(isOverdue(item))due=360;else if(isDueToday(item))due=240;else{const d=dateObject(item.dueAt);if(d){const days=(d.getTime()-Date.now())/86400000;if(days<=2)due=150;else if(days<=5)due=80;}}return p+s+due+(item.relation==="mine"?80:item.isFollowing?30:0);}
function todayIds(items){return new Set([...items].filter(x=>!isCompleted(x)&&["mine","created","participant"].includes(x.relation)).sort((a,b)=>todayScore(b)-todayScore(a)).slice(0,12).map(x=>String(x.id)));}
function metrics(items){const related=items.filter(x=>["mine","created","participant"].includes(x.relation));const a=computeWorkAttention(related,identity().subjectId||"");return {total:items.length,mine:items.filter(x=>x.relation==="mine"&&!isCompleted(x)).length,active:items.filter(x=>["in_progress","active"].includes(x.status)).length,blocked:items.filter(x=>x.status==="blocked").length,review:items.filter(x=>x.status==="waiting").length,overdue:items.filter(isOverdue).length,completed:items.filter(x=>x.status==="completed").length,following:items.filter(x=>x.isFollowing&&!isCompleted(x)).length,attention:a.count,today:todayIds(items).size};}
function workReason(item){if(isOverdue(item))return "已超过截止时间，建议优先处理";if(item.status==="blocked")return item.metadata?.blockReason||item.metadata?.waitingReason||"当前工作存在阻塞，需要先解除问题";if(item.priority==="urgent")return "紧急工作，建议立即确认下一步";if(isDueToday(item))return "今天到期，需要在计划时间内推进";if(["in_progress","active"].includes(item.status))return "工作已经开始，建议保持连续推进";if(item.status==="waiting")return "执行结果已提交，等待验收确认";return item.metadata?.workReason||item.goalSummary||item.description||"当前计划工作";}
function nextAction(item){return item.metadata?.nextAction||item.nextAction||(item.status==="pending"?"开始处理，并记录第一步结果":item.status==="blocked"?"确认阻塞原因，选择解决路径后继续推进":item.status==="waiting"?"检查执行结果与证据，决定是否验收":["in_progress","active"].includes(item.status)?"继续推进当前执行，并记录最新进展":item.status==="completed"?"查看闭环结果与执行证据":"确认下一步行动");}
function acceptanceCriteria(item){return item.metadata?.acceptanceCriteria||item.acceptanceCriteria||item.expectedResult||"待补充：明确怎样才算真正完成";}
function contextLabel(item){return item.metadata?.sourcePage||item.workbenchCode||item.relatedObjectId||"工作之家";}
function estimatedText(item){const n=Number(item.metadata?.estimatedMinutes||item.estimatedMinutes||0);return Number.isFinite(n)&&n>0?`预计 ${n}分钟`:"";}
function updatedText(item){return item.updatedAt?`更新 ${dateText(item.updatedAt)}`:"";}
function scopeEndpoint(){const id=routeId();if(id==="work-following")return FOLLOWING_WORK_ENDPOINT;if(id==="work-today"||id==="work-mine"||id==="work-pending"||id==="work-active"||id==="work-waiting"||id==="work-completed")return RELATED_WORK_ENDPOINT;return ALL_WORK_ENDPOINT;}
function routeMatches(item,allItems){const id=routeId();if(id==="work-today")return todayIds(allItems).has(String(item.id));if(id==="work-following")return item.isFollowing;if(BUSINESS_ROUTE_MAP[id])return inferBusinessCode(item)===BUSINESS_ROUTE_MAP[id];if(id==="work-mine")return item.relation==="mine";if(id==="work-blocked")return item.status==="blocked"||hasWaitingSignal(item);if(id==="work-review")return item.status==="waiting";if(id==="work-records")return ["completed","cancelled","archived"].includes(item.status);if(id==="work-pending")return item.status==="pending";if(id==="work-active")return ["in_progress","active"].includes(item.status);if(id==="work-waiting")return item.status==="waiting";if(id==="work-completed")return item.status==="completed";return true;}
function searchMatches(item,q){if(!q)return true;return [item.title,item.description,item.goalSummary,item.expectedResult,item.workbenchCode,item.relatedObjectType,item.relatedObjectId,item.metadata?.sourcePage,item.metadata?.nextAction,item.metadata?.workReason,sourceLabel(item),businessLabel(item),projectLabel(item),teamLabel(item),ownerLabel(item)].join(" ").toLowerCase().includes(q.toLowerCase());}
function relationMatches(item,v){if(!v||v==="all")return true;if(v==="mine")return item.relation==="mine";if(v==="created")return String(item.createdByPersonId||"")===String(identity().subjectId||"");if(v==="following")return item.isFollowing;if(v==="participant")return item.isParticipant&&!item.isFollowing;if(v==="ai")return sourceLabel(item)==="美和AI";return true;}
function statusMatches(item,v){if(!v||v==="all")return true;if(v==="in_progress")return ["in_progress","active"].includes(item.status);return item.status===v;}
function priorityMatches(item,v){return !v||v==="all"||item.priority===v;}
function businessMatches(item,v){return !v||v==="all"||inferBusinessCode(item)===v;}
function teamMatches(item,v){return !v||v==="all"||teamLabel(item)===v;}
function projectMatches(item,v){return !v||v==="all"||projectLabel(item)===v;}
function sortItems(items,sort){const arr=[...items];if(sort==="due")return arr.sort((a,b)=>(dateObject(a.dueAt)?.getTime()||Number.MAX_SAFE_INTEGER)-(dateObject(b.dueAt)?.getTime()||Number.MAX_SAFE_INTEGER));if(sort==="updated")return arr.sort((a,b)=>(dateObject(b.updatedAt)?.getTime()||0)-(dateObject(a.updatedAt)?.getTime()||0));if(sort==="priority")return arr.sort((a,b)=>({urgent:4,high:3,normal:2,low:1}[b.priority]||0)-({urgent:4,high:3,normal:2,low:1}[a.priority]||0));return arr.sort((a,b)=>todayScore(b)-todayScore(a));}

function routeNote(id){
  if(id==="work-all")return "全部工作是统一工作事项入口。公司内部默认开放查看，敏感事项例外；搜索、筛选、排序与卡片/列表用于管理大量工作。";
  if(id==="work-following")return "关注不是接手任务。关注后可以持续看到该工作最新状态与进展，不需要靠会议或口头同步。";
  if(BUSINESS_ROUTE_MAP[id])return `${BUSINESS_BY_ID[BUSINESS_ROUTE_MAP[id]]?.name||"当前事业"}工作只聚合该事业的工作事项；跨事业内容仍可通过“全部工作”查看或主动关注。`;
  if(id==="work-today")return "今日工作只回答“现在先做什么”。首版按优先级、截止时间、状态和与你的关系排序，后续继续接入工作量与人/AI产能。";
  if(id==="work-blocked")return "聚合真正需要重新关注的等待与阻塞事项；等待是正常业务状态，阻塞是需要解决的问题。";
  if(id==="work-review")return "执行完成不等于闭环。这里集中处理已提交结果、等待确认的工作。";
  if(id==="work-records")return "已闭环与已取消工作进入历史记录，保留执行证据、结果事实和组织记忆。";
  return "一份工作事实，多种视图；看得到不等于要处理，也不等于可以修改。";
}
function aside(items){const m=metrics(items),id=routeId();window.dispatchEvent(new CustomEvent("aione:page-aside-context",{detail:{state:"standard",kicker:"工作之家 V2.0",title:ROUTE_TITLES[id]||"工作之家",text:routeNote(id),items:[{label:"当前可见",value:`${items.length}项`},{label:"我的关注",value:`${m.following}项`},{label:"需要关注",value:`${m.attention}项`},{label:"原则",value:"看得到 ≠ 要处理 ≠ 能操作"}]}}));}
async function loadItems(){const personId=identity().subjectId||"";try{const r=await aioneApi(scopeEndpoint());return {items:(r.items||[]).map(x=>normalizeRemote(x,r.personId||personId)),mode:"database"};}catch(error){const local=getCollaborationData();return {items:(local.tasks||[]).map(x=>normalizeLocal(x,personId)),mode:"local",error};}}

function followButton(item,compact=false){return `<button class="miwa-work-follow${item.isFollowing?" is-following":""}" type="button" data-work-follow="${esc(item.id)}" aria-pressed="${item.isFollowing?"true":"false"}" title="${item.isFollowing?"取消关注":"关注工作动态"}">${item.isFollowing?"★ 已关注":"☆ 关注"}${compact?"":""}</button>`;}
function cardHtml(item){const href=sourceHref(item),context=contextLabel(item),sourceNode=href?`<a class="miwa-work-source-link" href="${esc(href)}">${esc(context)}</a>`:esc(context),risk=isOverdue(item)||item.status==="blocked"||item.priority==="urgent"?" is-risk":"",est=estimatedText(item),project=projectLabel(item),team=teamLabel(item);return `<article class="miwa-work-card${risk}" data-work-item-id="${esc(item.id)}"><div class="miwa-work-card__head"><div class="miwa-work-card__tags"><span class="miwa-work-relation">${esc(relationLabel(item))}</span><span class="miwa-work-status">${esc(statusLabel(item.status))}</span><span class="miwa-work-priority">${esc(priorityLabel(item.priority))}</span></div>${followButton(item,true)}</div><div class="miwa-work-card__title"><strong>${esc(item.title||"未命名工作")}</strong><p>${esc(workReason(item))}</p></div><div class="miwa-work-card__scope"><span>${esc(businessLabel(item))}</span>${project?`<span>${esc(project)}</span>`:""}${team?`<span>${esc(team)}</span>`:""}</div><div class="miwa-work-next"><span>下一步</span><strong>${esc(nextAction(item))}</strong></div><div class="miwa-work-card__meta"><span>${esc(ownerLabel(item))}</span><span>${item.dueAt?`截止 ${esc(dateText(item.dueAt))}`:"无明确截止"}</span>${est?`<span>${esc(est)}</span>`:""}<span>${esc(updatedText(item))}</span></div><div class="miwa-work-card__footer"><small>${sourceNode}${sourceLabel(item)==="美和AI"?" · 美和AI整理":""}</small><button class="miwa-work-primary" type="button" data-work-open="${esc(item.id)}">${item.status==="waiting"?"验收":"处理"}</button></div></article>`;}
function listRowHtml(item){const project=projectLabel(item),team=teamLabel(item);return `<tr data-work-item-id="${esc(item.id)}"><td><span class="miwa-work-priority-dot ${item.priority}"></span>${esc(priorityLabel(item.priority))}</td><td><button class="miwa-work-title-link" type="button" data-work-open="${esc(item.id)}"><strong>${esc(item.title||"未命名工作")}</strong><small>${esc(nextAction(item))}</small></button></td><td><strong>${esc(businessLabel(item))}</strong><small>${esc(project||team||contextLabel(item))}</small></td><td>${esc(ownerLabel(item))}<small>${esc(relationLabel(item))}</small></td><td><span class="miwa-work-table-status">${esc(statusLabel(item.status))}</span></td><td>${item.dueAt?esc(dateText(item.dueAt)):"—"}<small>${isOverdue(item)?"已超期":esc(updatedText(item))}</small></td><td>${followButton(item,true)}</td><td><button class="miwa-work-table-action" type="button" data-work-open="${esc(item.id)}">${item.status==="waiting"?"验收":"处理"}</button></td></tr>`;}
function browserRowsHtml(items,view){if(!items.length)return `<div class="miwa-work-empty">当前条件下暂无工作事项。</div>`;if(view==="list")return `<div class="miwa-work-table-wrap"><table class="miwa-work-table"><thead><tr><th>优先</th><th>工作事项 / 下一步</th><th>事业 / 项目</th><th>负责人</th><th>状态</th><th>时间</th><th>关注</th><th>操作</th></tr></thead><tbody>${items.map(listRowHtml).join("")}</tbody></table></div>`;return `<div class="miwa-work-card-grid">${items.map(cardHtml).join("")}</div>`;}
function optionsHtml(items,getter,placeholder){const values=[...new Set(items.map(getter).filter(Boolean))].sort((a,b)=>String(a).localeCompare(String(b),"zh-CN"));return `<option value="all">${esc(placeholder)}</option>${values.map(v=>`<option value="${esc(v)}">${esc(v)}</option>`).join("")}`;}
function businessOptionsHtml(){return `<option value="all">全部事业</option><option value="group">集团 / AIONE</option>${MIWA_BUSINESSES.map(b=>`<option value="${esc(b.id)}">${esc(b.name)}</option>`).join("")}`;}
function browserShellHtml(){const id=routeId(),isToday=id==="work-today";return `<section class="miwa-work-home"><header class="miwa-work-hero"><div class="miwa-work-hero__eyebrow">MIWA GROUP WORK · V2.0</div><div class="miwa-work-hero__row"><div><h1>${esc(ROUTE_TITLES[id]||"工作之家")}</h1><p>${esc(routeNote(id))}</p></div><a href="#/work" class="miwa-work-guide-link">查看工作手册</a></div></header><div class="miwa-work-kpis" data-work-kpis></div><section class="miwa-work-browser"><div class="miwa-work-browser__toolbar"><div class="miwa-work-searchbox"><span>⌕</span><input class="miwa-work-search" data-work-search placeholder="搜索工作名称、下一步、业务对象、负责人…"></div><select data-work-relation><option value="all">全部关系</option><option value="mine">我负责</option><option value="participant">我参与</option><option value="created">我创建</option><option value="following">我关注</option><option value="ai">美和AI创建</option></select><select data-work-status>${STATUS_FILTERS.map(([v,l])=>`<option value="${v}">${v==="all"?"全部状态":l}</option>`).join("")}</select><select data-work-priority>${PRIORITY_FILTERS.map(([v,l])=>`<option value="${v}">${l}</option>`).join("")}</select><select data-work-business>${businessOptionsHtml()}</select><button class="miwa-work-more-filter" type="button" data-work-more-filter>更多筛选</button><div class="miwa-work-toolbar-spacer"></div><select data-work-sort>${SORT_OPTIONS.map(([v,l])=>`<option value="${v}">${l}</option>`).join("")}</select><div class="miwa-work-view-toggle" role="group" aria-label="工作视图"><button type="button" data-work-view="card" class="is-active">卡片</button><button type="button" data-work-view="list">列表</button></div><button class="miwa-work-refresh" type="button" data-work-refresh aria-label="刷新" title="刷新">↻</button></div><div class="miwa-work-browser__advanced" data-work-advanced hidden><label>团队<select data-work-team><option value="all">全部团队</option></select></label><label>项目<select data-work-project><option value="all">全部项目</option></select></label><p>工作关系、事业、团队、项目只是同一 Work Item 的不同观察维度，不创建第二套任务。</p></div><div class="miwa-work-browser__summary"><div><h2>${isToday?"现在先做":"工作事项"}</h2><p>${isToday?"美和AI把与你真正相关、今天最值得推进的工作排在前面。":"卡片用于快速理解，列表用于大量比较；搜索和筛选负责找到真正需要看的工作。"}</p></div><span data-work-count></span></div><div data-work-error></div><div data-work-list><div class="miwa-work-loading">正在读取工作事项…</div></div></section><dialog class="miwa-work-detail" data-work-detail><div class="miwa-work-detail-body" data-work-detail-body></div></dialog></section>`;}

function publicationNineQuestions(){const nine=[["01","要做什么？","工作内容"],["02","为什么要做？","原因与业务价值"],["03","属于哪里？","事业、流程与工作台"],["04","和什么有关？","商品、订单、客户等对象"],["05","谁来负责？","最终负责人和执行者"],["06","现在到哪一步？","当前状态"],["07","应该先做还是后做？","优先级"],["08","下一步做什么？","Next Action"],["09","怎样才算真正完成？","验收标准与闭环"]];return `<div class="miwa-publication-editorial-grid cols-3 compact">${nine.map(([n,q,a],i)=>`<article class="miwa-publication-editorial-card is-compact ${i%3===1?"tone-red":"tone-green"}"><span>${n}</span><h3>${esc(q)}</h3><p>${esc(a)}</p></article>`).join("")}</div>`;}
function overviewHtml(items,mode){const m=metrics(items),businessCounts=MIWA_BUSINESSES.map(b=>[b.name,items.filter(x=>inferBusinessCode(x)===b.id&&!isCompleted(x)).length]).filter(([,n])=>n>0).slice(0,6);const cover=publicationCover({title:"工作之家",englishTitle:"MIWA WORK HOME",subtitle:"美和集团所有需要被完成的工作的统一入口与执行中枢。",statement:"不遗漏 · 不混乱 · 有优先级 · 可推进 · 可验收 · 可闭环",bookLabel:"工作手册",visualHtml:`<span class="miwa-work-book-cover-icon" data-icon="work" aria-hidden="true"></span>`});const back=publicationBackCover({title:"关于《美和工作手册》",summary:"本册用于帮助所有成员理解一件工作如何进入AIONE、如何按事业与关系组织、如何由人和美和AI持续推进，以及怎样真正闭环。",contents:["工作之家是什么","为什么需要工作之家","美和工作9问","工作如何流动","人与美和AI","工作如何组织","工作之家与日历","当前工作动态"],audiences:["集团成员","事业负责人","项目参与者","AI与自动化设计者"],bookLabel:"工作手册",visualHtml:`<span class="miwa-work-book-cover-icon" data-icon="work" aria-hidden="true"></span>`});const page2=publicationPage({pageNumber:2,section:"第01章｜工作之家是什么",title:"工作之家不是任务仓库，而是美和集团统一的工作事实与执行入口",lead:"无论工作来自人、美和AI、业务流程、异常还是未来外部系统，只要需要采取行动并产生结果，就进入同一 Work Item。",content:`<div class="miwa-publication-editorial-grid cols-3"><article class="miwa-publication-editorial-card tone-green"><span>01</span><h3>一份工作事实</h3><p>工作台、事业、项目、团队和个人查看的是同一条 Work Item，不重复建任务。</p></article><article class="miwa-publication-editorial-card tone-red"><span>02</span><h3>多种观察视图</h3><p>今日工作、全部工作、事业工作与关注动态只是不同视图，不改变底层事实。</p></article><article class="miwa-publication-editorial-card tone-green"><span>03</span><h3>真正闭环</h3><p>执行完成后还要经过结果、证据、验收和业务状态同步，才能成为已闭环工作。</p></article></div>`,conclusion:"后台负责结构化与自动整理，前台只让人清楚知道公司在做什么、跟自己有什么关系、下一步是什么。",bookLabel:"工作手册"});const page3=publicationPage({pageNumber:3,section:"第02章｜为什么需要工作之家",title:"工作越来越多以后，真正危险的不是忙，而是遗漏、混乱和失去闭环",lead:"AIONE不要求员工把精力花在整理系统上。美和AI持续整理，人负责需要判断、执行和承担结果的部分。",content:`<div class="miwa-publication-editorial-grid cols-2"><article class="miwa-publication-editorial-card tone-green"><span>不遗漏</span><h3>所有需要行动的事情统一收口</h3><p>等待、阻塞、待验收和下一次检查都由系统持续跟踪，不依赖个人记忆。</p></article><article class="miwa-publication-editorial-card tone-red"><span>不混乱</span><h3>状态、优先级、责任、可见性分别管理</h3><p>看得到 ≠ 要处理 ≠ 能操作；透明不等于把所有工作都推送给所有人。</p></article><article class="miwa-publication-editorial-card tone-green"><span>有重点</span><h3>今日工作不是今天到期，而是今天合理应该推进</h3><p>优先级、时间、依赖、经营影响和未来产能共同决定工作顺序。</p></article><article class="miwa-publication-editorial-card tone-red"><span>有结果</span><h3>完成不等于闭环</h3><p>没有结果事实、证据和验收的“已完成”，不能成为组织经营证据。</p></article></div>`,conclusion:"系统负责放大正确的方法，而不是放大混乱。",bookLabel:"工作手册"});const page4=publicationPage({pageNumber:4,section:"第03章｜美和工作9问",title:"九个问题，把一件工作整理到可执行、可推进、可验收、可闭环",lead:"美和业务9要素用于理解业务对象；美和工作9问用于把需要做的工作讲清楚。",content:publicationNineQuestions(),conclusion:"任何活跃工作至少要能够回答：下一步是什么？怎样才算真正完成？",bookLabel:"工作手册"});const flow=["发现工作","AI整理","确定责任","判断优先","明确下一步","执行推进","等待 / 阻塞","验收","闭环"];const page5=publicationPage({pageNumber:5,section:"第04章｜工作如何流动",title:"从发现到闭环，每一步都有明确状态和责任",lead:"状态机保持简单，风险、超期和优先级作为属性存在，不把所有概念都塞进状态。",content:`<div class="miwa-publication-process-grid">${flow.map((x,i)=>`<span><b>${String(i+1).padStart(2,"0")}</b>${esc(x)}</span>`).join("")}</div><div class="miwa-publication-editorial-note"><strong>关键规则</strong><p>WAITING 是正常等待；BLOCKED 是异常阻塞。CLOSED 原则上不重新打开，新问题建立新的关联工作事项。</p></div>`,conclusion:"流程负责推动业务状态，Work Item 负责推动人和AI行动。",bookLabel:"工作手册"});const page6=publicationPage({pageNumber:6,section:"第05章｜人与美和AI",title:"员工负责完成工作，美和AI负责让工作系统长期保持整洁",lead:"AI不是聊天框，而是持续运行的整理层：识别、去重、分类、排序、提醒、发现阻塞、准备下一步和辅助验收。",content:`<div class="miwa-publication-editorial-grid cols-2"><article class="miwa-publication-editorial-card tone-green"><span>HUMAN</span><h3>人负责目标、关键判断、重大异常与最终结果</h3><p>AI可以执行和建议，但关键业务必须保留明确的人类最终负责人。</p></article><article class="miwa-publication-editorial-card tone-red"><span>MIWA AI</span><h3>AI负责持续整理、辅助判断与可标准化执行</h3><p>能够规则化的规则化，能够自动化的自动化，需要判断的交给AI，需要负责的留给人。</p></article></div>`,conclusion:"员工不负责维护复杂任务系统；AI先整理，只有无法确定的事实才向人请求确认。",bookLabel:"工作手册"});const page7=publicationPage({pageNumber:7,section:"第06章｜工作如何组织",title:"事业是主要工作边界；全部工作保持集团透明，关注负责跨边界连接",lead:"跨境团队日常优先看美和跨境，美和留学成员优先看留学；感兴趣或相关的跨事业工作可以主动关注。",content:`<div class="miwa-publication-editorial-grid cols-3"><article class="miwa-publication-editorial-card tone-green"><span>事业</span><h3>默认工作视野</h3><p>按美和跨境、美和批发、美和留学等事业聚合工作，降低不相关信息干扰。</p></article><article class="miwa-publication-editorial-card tone-red"><span>全部工作</span><h3>集团透明入口</h3><p>公司内部默认可以了解其他事业在做什么；敏感事项例外，操作权限仍单独判断。</p></article><article class="miwa-publication-editorial-card tone-green"><span>关注</span><h3>主动建立相关性</h3><p>关注不等于接手任务。关注后持续看到该工作的最新状态和动态，减少会议式广播。</p></article></div>`,conclusion:"事业解决默认边界，关注解决主动跨界，全部工作解决组织透明。",bookLabel:"工作手册"});const page8=publicationPage({pageNumber:8,section:"第07章｜工作之家与美和日历",title:"Work Item 管“做什么”，美和日历管“什么时候做”",lead:"美和日历不是第二套任务系统，而是工作事项在时间维度上的投影。",content:`<div class="miwa-publication-editorial-grid cols-2"><article class="miwa-publication-editorial-card tone-green"><span>WORK</span><h3>工作之家</h3><p>负责人、业务归属、下一步、优先级、等待/阻塞、验收和闭环。</p></article><article class="miwa-publication-editorial-card tone-red"><span>TIME</span><h3>美和日历</h3><p>工作时间块、关键截止点、next_review_at 检查节点和工作进程时间线。</p></article></div><div class="miwa-publication-editorial-note"><strong>一份数据，多种视图</strong><p>修改工作计划时间后，日历自动变化；拖动日历安排，本质上仍更新同一 Work Item 的计划时间。</p></div>`,conclusion:"不是所有工作都进入日历；只有有明确时间安排、截止点、检查节点或重要里程碑的工作才投影。",bookLabel:"工作手册"});const businessHtml=businessCounts.length?businessCounts.map(([name,count],i)=>`<article class="miwa-publication-editorial-card is-compact ${i%2?"tone-red":"tone-green"}"><span>${String(i+1).padStart(2,"0")}</span><h3>${esc(name)}</h3><strong>${count} 项活跃工作</strong><p>进入对应事业工作页查看明细。</p></article>`).join(""):`<article class="miwa-publication-editorial-card tone-green is-feature"><span>当前</span><h3>事业工作归属仍在持续补齐</h3><p>现有历史 Work Item 未必全部带有事业标识；新工作将按事业、项目、团队和关系逐步完善。</p></article>`;const page9=publicationPage({pageNumber:9,section:"第08章｜当前工作动态",title:"让组织自己保持同步，而不是靠反复开会广播信息",lead:`${mode==="database"?"当前读取AIONE正式工作事实。":"正式Backend暂未连接，当前使用本地预览工作记录。"} 工作动态页用于让成员快速知道公司正在推进什么、哪里阻塞、最近有什么成果。`,content:`<div class="miwa-work-publication-kpis"><div><span>当前可见</span><strong>${m.total}</strong></div><div><span>今日建议</span><strong>${m.today}</strong></div><div><span>我的关注</span><strong>${m.following}</strong></div><div class="${m.blocked?"is-risk":""}"><span>阻塞</span><strong>${m.blocked}</strong></div><div><span>待验收</span><strong>${m.review}</strong></div><div><span>已闭环</span><strong>${m.completed}</strong></div></div><div class="miwa-publication-editorial-grid cols-2 compact">${businessHtml}</div>`,conclusion:"工作概览负责让人理解；今日工作负责让人执行；事业工作负责默认边界；全部工作与关注负责组织透明与跨边界协作。",bookLabel:"工作手册"});return `<article class="miwa-work-manual miwa-publication-book" data-publication-book="work-home">${publicationCoverSpread(cover,back)}${publicationSpread(page2,page3,"work-chapters-01-02")}${publicationSpread(page4,page5,"work-chapters-03-04")}${publicationSpread(page6,page7,"work-chapters-05-06")}${publicationSpread(page8,page9,"work-chapters-07-08")}</article>`;}

function evidenceTypeLabel(v){return ({completion:"完成证据",review:"确认记录",ai_review:"美和AI复盘",execution_note:"执行记录",link:"证据链接",file:"文件证据"})[v]||v||"执行记录";}
function safeHref(v){try{const u=new URL(String(v||""),window.location.href);return ["http:","https:"].includes(u.protocol)?u.href:"";}catch(_){return "";}}
function publishWorkContext(detail=null){window.AIONEWorkExecutionContext=detail?{generatedAt:new Date().toISOString(),workItem:detail.workItem,evidence:detail.evidence||[],results:detail.results||[],permissions:detail.permissions||{},following:Boolean(detail.following)}:null;window.dispatchEvent(new CustomEvent("aione:work-detail-context-change",{detail:window.AIONEWorkExecutionContext}));}
function evidenceHtml(items=[]){if(!items.length)return `<div class="miwa-work-detail-empty">还没有执行证据。开始执行后，可持续记录说明、链接、文件位置和关键结果。</div>`;return items.map(i=>`<article class="miwa-work-evidence"><div><strong>${esc(evidenceTypeLabel(i.evidenceType))}</strong><span>${esc(dateText(i.happenedAt||i.createdAt))}</span></div><p>${esc(i.summary||"—")}</p>${safeHref(i.evidenceUri)?`<a href="${esc(safeHref(i.evidenceUri))}" target="_blank" rel="noopener">打开证据 ↗</a>`:""}</article>`).join("");}
function resultsHtml(items=[]){if(!items.length)return `<div class="miwa-work-detail-empty">尚未形成结果事实。</div>`;return items.map(i=>`<article class="miwa-work-result"><div><strong>${esc(i.resultType||"工作结果")}</strong><span>${esc(i.status||"observed")}</span></div><p>${esc(i.textValue||i.numericValue||"—")}</p></article>`).join("");}
function workNineHtml(item){const where=[businessLabel(item),item.workbenchCode||item.metadata?.sourcePage].filter(Boolean).join(" · ")||"待补充业务归属",related=[item.relatedObjectType,item.relatedObjectId].filter(Boolean).join(" · ")||"待补充关联对象",qs=[["01","要做什么？",item.title||"待补充工作标题"],["02","为什么要做？",item.metadata?.workReason||item.goalSummary||item.description||"待补充工作原因"],["03","属于哪里？",where],["04","和什么有关？",related],["05","谁来负责？",ownerLabel(item)],["06","现在到哪一步？",statusLabel(item.status)],["07","应该先做还是后做？",`${priorityLabel(item.priority)}${isOverdue(item)?" · 已超期":""}`],["08","下一步做什么？",nextAction(item)],["09","怎样才算真正完成？",acceptanceCriteria(item)]];return `<section class="miwa-work-nine"><div class="miwa-work-nine__head"><div><span>MIWA WORK 9 QUESTIONS</span><h3>美和工作9问</h3></div><p>用9个问题确认工作是否已经清楚到可执行、可推进、可验收、可闭环。</p></div><div class="miwa-work-nine__grid">${qs.map(([n,q,a])=>`<article><span>${n}</span><h4>${esc(q)}</h4><p>${esc(a)}</p></article>`).join("")}</div></section>`;}
function detailHtml(detail){const item=detail.workItem||{},p=detail.permissions||{};return `<div class="miwa-work-detail-head"><div><span>WORK EXECUTION</span><h2>${esc(item.title||"工作事项")}</h2><p>${esc(item.description||item.goalSummary||"暂无补充说明")}</p></div><div class="miwa-work-detail-head__actions">${followButton({...item,isFollowing:Boolean(detail.following)},true)}<button type="button" class="miwa-work-detail-close" data-work-detail-close aria-label="关闭">×</button></div></div><div class="miwa-work-detail-meta"><span>状态 <b>${esc(statusLabel(item.status))}</b></span><span>优先级 <b>${esc(priorityLabel(item.priority))}</b></span><span>负责人 <b>${esc(ownerLabel(item))}</b></span><span>事业 <b>${esc(businessLabel(item))}</b></span><span>关系 <b>${esc(relationLabel({...item,isFollowing:Boolean(detail.following),isParticipant:Boolean(detail.participantRoles?.length),participantRole:detail.participantRoles?.[0]||""}))}</b></span></div><div class="miwa-work-detail-timeline"><span class="${["in_progress","waiting","completed"].includes(item.status)?"is-done":"is-current"}">待开始</span><span class="${["waiting","completed"].includes(item.status)?"is-done":item.status==="in_progress"?"is-current":""}">执行中</span><span class="${item.status==="completed"?"is-done":item.status==="waiting"?"is-current":""}">待验收</span><span class="${item.status==="completed"?"is-current":""}">已闭环</span></div>${workNineHtml(item)}<div class="miwa-work-detail-grid"><section><h3>执行证据</h3><div class="miwa-work-evidence-list">${evidenceHtml(detail.evidence)}</div></section><section><h3>结果事实</h3><div class="miwa-work-result-list">${resultsHtml(detail.results)}</div>${item.resultSummary?`<div class="miwa-work-result-summary"><b>当前结果</b><p>${esc(item.resultSummary)}</p></div>`:""}</section></div><div class="miwa-work-detail-actions">${p.canStart?`<button class="primary" type="button" data-work-action="start">开始执行</button>`:""}${p.canAddEvidence?`<button type="button" data-work-toggle="evidence">添加执行记录</button>`:""}${p.canSubmitCompletion?`<button type="button" data-work-toggle="complete">提交完成</button>`:""}${p.canApproveCompletion?`<button class="primary" type="button" data-work-toggle="approve">确认完成</button>`:""}${p.canAIReview?`<button type="button" data-work-action="ai-review">让美和AI复盘</button>`:""}${!p.canStart&&!p.canAddEvidence&&!p.canSubmitCompletion&&!p.canApproveCompletion?`<span class="miwa-work-readonly-note">当前为查看/关注关系，无执行权限。</span>`:""}</div><form class="miwa-work-detail-form" data-work-evidence-form hidden><label>执行说明<textarea name="summary" rows="3" placeholder="记录做了什么、发生了什么、下一步是什么"></textarea></label><label>证据链接 / 文件地址（可选）<input name="evidenceUri" placeholder="Google Drive、AIONE页面或其他可追溯地址"></label><div><button type="button" data-work-form-cancel="evidence">取消</button><button class="primary" type="submit">保存执行记录</button></div></form><form class="miwa-work-detail-form" data-work-complete-form hidden><label>执行结果<textarea name="resultSummary" rows="4" required placeholder="明确说明完成了什么、结果是否达到目标、还有什么遗留问题"></textarea></label><label>完成证据说明（可选）<textarea name="evidenceSummary" rows="2"></textarea></label><label>证据链接 / 文件地址（可选）<input name="evidenceUri"></label><div><button type="button" data-work-form-cancel="complete">取消</button><button class="primary" type="submit">提交结果</button></div></form><form class="miwa-work-detail-form" data-work-approve-form hidden><label>确认意见<textarea name="reviewSummary" rows="3">确认执行结果，工作完成。</textarea></label><div><button type="button" data-work-form-cancel="approve">取消</button><button class="primary" type="submit">确认完成</button></div></form>`;}

export async function initMiwaWorkHome(){
  const host=document.getElementById("miwa-work-home-entry");if(!host)return false;
  publishWorkContext(null);
  const id=routeId(),isOverview=id==="work";
  setPublicationPageMode(isOverview);
  let allItems=[],mode="database",activeDetail=null,view=localStorage.getItem("aione.work.view.v1")||"card",filters={relation:"all",status:"all",priority:"all",business:BUSINESS_ROUTE_MAP[id]||"all",team:"all",project:"all",sort:id==="work-following"?"updated":"ai"};
  host.innerHTML=isOverview?`<section class="miwa-work-home"><div class="miwa-work-loading">正在装订《美和工作手册》…</div></section>`:browserShellHtml();
  let list=host.querySelector("[data-work-list]"),kpis=host.querySelector("[data-work-kpis]"),count=host.querySelector("[data-work-count]"),search=host.querySelector("[data-work-search]"),relationFilter=host.querySelector("[data-work-relation]"),statusFilter=host.querySelector("[data-work-status]"),priorityFilter=host.querySelector("[data-work-priority]"),businessFilter=host.querySelector("[data-work-business]"),teamFilter=host.querySelector("[data-work-team]"),projectFilter=host.querySelector("[data-work-project]"),sortFilter=host.querySelector("[data-work-sort]"),errorHost=host.querySelector("[data-work-error]"),dialog=host.querySelector("[data-work-detail]"),detailBody=host.querySelector("[data-work-detail-body]");
  if(businessFilter&&BUSINESS_ROUTE_MAP[id]){businessFilter.value=BUSINESS_ROUTE_MAP[id];businessFilter.disabled=true;}
  if(sortFilter)sortFilter.value=filters.sort;
  function syncDynamicFilters(){if(teamFilter){const prev=teamFilter.value||"all";teamFilter.innerHTML=optionsHtml(allItems,teamLabel,"全部团队");teamFilter.value=[...teamFilter.options].some(o=>o.value===prev)?prev:"all";}if(projectFilter){const prev=projectFilter.value||"all";projectFilter.innerHTML=optionsHtml(allItems,projectLabel,"全部项目");projectFilter.value=[...projectFilter.options].some(o=>o.value===prev)?prev:"all";}}
  function rows(){const q=search?.value?.trim()||"";let r=allItems.filter(x=>routeMatches(x,allItems)).filter(x=>searchMatches(x,q)).filter(x=>relationMatches(x,filters.relation)).filter(x=>statusMatches(x,filters.status)).filter(x=>priorityMatches(x,filters.priority)).filter(x=>businessMatches(x,filters.business)).filter(x=>teamMatches(x,filters.team)).filter(x=>projectMatches(x,filters.project));return sortItems(r,filters.sort);}
  function renderList(){const m=metrics(allItems);if(kpis)kpis.innerHTML=[["今日建议",m.today,""],["当前可见",m.total,""],["我的关注",m.following,""],["进行中",m.active,""],["阻塞",m.blocked,m.blocked?"is-risk":""],["待验收",m.review,""]].map(([l,v,c])=>`<div class="miwa-work-kpi ${c}"><span>${l}</span><strong>${v}</strong></div>`).join("");const r=rows();if(count)count.textContent=`${r.length} 项 · ${view==="card"?"卡片":"列表"}`;if(list)list.innerHTML=browserRowsHtml(r,view);host.querySelectorAll("[data-work-view]").forEach(b=>b.classList.toggle("is-active",b.dataset.workView===view));window.MIWAHeader?.setWorkCount?.(m.attention);aside(allItems);if(errorHost)errorHost.innerHTML=mode==="local"?`<div class="miwa-work-error">正式数据库暂时未连接，当前显示本地预览工作记录；关注仅保存在本机，执行闭环需要AIONE Backend。</div>`:"";}
  async function refresh(){if(list)list.innerHTML=`<div class="miwa-work-loading">正在读取工作事项…</div>`;const loaded=await loadItems();allItems=loaded.items;mode=loaded.mode;if(isOverview){host.innerHTML=overviewHtml(allItems,mode);renderSemanticIcons(host);bindPublicationActions(host);configurePublicationAside({kicker:"工作之家",title:"工作概览",summary:"按美和数字出版设计系统阅读。工作概览是长期工作手册 + 当前工作动态，不承担高频操作。",bookTitle:"美和工作手册",chapter:"概览 + 第01—08章"});validatePublicationPages(host);window.MIWAHeader?.setWorkCount?.(metrics(allItems).attention);return;}syncDynamicFilters();renderList();}
  async function toggleFollow(workId,nextValue=null){const item=allItems.find(x=>String(x.id)===String(workId));if(!item)return;const target=nextValue??!item.isFollowing;try{if(mode==="database"){await aioneApi(`/api/v1/work-home/${encodeURIComponent(workId)}/follow`,{method:target?"POST":"DELETE",body:target?"{}":undefined});}else{const ids=localFollowingIds();if(target)ids.add(String(workId));else ids.delete(String(workId));saveLocalFollowing(ids);}item.isFollowing=target;item.relation=item.relation==="mine"||item.relation==="created"?item.relation:target?"following":"shared";announceWorkItemsChanged({reason:target?"work-followed":"work-unfollowed",workItemId:workId});if(activeDetail&&String(activeDetail.workItem?.id)===String(workId)){activeDetail.following=target;detailBody.innerHTML=detailHtml(activeDetail);bindDetailActions();}await refresh();}catch(e){workError(e);}}
  async function loadDetail(workId){if(mode!=="database")throw new Error("正式执行闭环需要连接AIONE Backend。");const d=await aioneApi(`/api/v1/work-home/${encodeURIComponent(workId)}/execution`);activeDetail=d;publishWorkContext(d);detailBody.innerHTML=detailHtml(d);bindDetailActions();if(!dialog.open)dialog.showModal();}
  async function reloadActiveDetail(){if(activeDetail?.workItem?.id)await loadDetail(activeDetail.workItem.id);await refresh();}
  function closeDetail(){dialog.close();activeDetail=null;publishWorkContext(null);}
  function setForm(name,visible){const f=detailBody.querySelector(`[data-work-${name}-form]`);if(f)f.hidden=!visible;}
  function workError(e){alert(e?.message||"工作执行失败，请稍后重试。");}
  async function startWork(){try{await aioneApi(`/api/v1/work-home/${encodeURIComponent(activeDetail.workItem.id)}/start`,{method:"POST",body:"{}"});announceWorkItemsChanged({reason:"work-started",workItemId:activeDetail.workItem.id});await reloadActiveDetail();}catch(e){workError(e);}}
  async function addEvidence(form){const d=new FormData(form),summary=String(d.get("summary")||"").trim(),evidenceUri=String(d.get("evidenceUri")||"").trim();try{await aioneApi(`/api/v1/work-home/${encodeURIComponent(activeDetail.workItem.id)}/evidence`,{method:"POST",body:JSON.stringify({summary,evidenceUri,evidenceType:evidenceUri?"link":"execution_note"})});announceWorkItemsChanged({reason:"work-evidence-added",workItemId:activeDetail.workItem.id});form.reset();await reloadActiveDetail();}catch(e){workError(e);}}
  async function completeWork(form){const d=new FormData(form),resultSummary=String(d.get("resultSummary")||"").trim(),evidenceSummary=String(d.get("evidenceSummary")||"").trim(),evidenceUri=String(d.get("evidenceUri")||"").trim();try{const r=await aioneApi(`/api/v1/work-home/${encodeURIComponent(activeDetail.workItem.id)}/complete`,{method:"POST",body:JSON.stringify({resultSummary,evidenceSummary,evidenceUri})});announceWorkItemsChanged({reason:"work-completion-submitted",workItemId:activeDetail.workItem.id,completionState:r.completionState||""});form.reset();await reloadActiveDetail();if(r.completionState==="waiting")alert("执行结果已提交，等待工作创建者确认。\n美和AI已经可以读取本次结果和证据进行复盘。");}catch(e){workError(e);}}
  async function approveWork(form){const d=new FormData(form),reviewSummary=String(d.get("reviewSummary")||"").trim();try{await aioneApi(`/api/v1/work-home/${encodeURIComponent(activeDetail.workItem.id)}/approve`,{method:"POST",body:JSON.stringify({reviewSummary})});announceWorkItemsChanged({reason:"work-completion-approved",workItemId:activeDetail.workItem.id});await reloadActiveDetail();}catch(e){workError(e);}}
  function aiReview(){publishWorkContext(activeDetail);window.MIWAAI?.open?.("work-home-review");const prompt="复盘当前工作结果。请基于工作目标、美和工作9问、原始Proposal来源、执行证据和结果事实判断：1）是否真正完成目标；2）有哪些遗留问题；3）是否应沉淀为规则、知识、Skill或自动化；4）是否需要创建下一轮工作。事实不足的地方标记待确认，不要编造。";window.setTimeout(()=>window.AIONEAISecretary?.sendCommand?.(prompt,{capabilityCode:"work.execution_review",capabilityLabel:"复盘工作结果"}),120);}
  function bindDetailActions(){detailBody.querySelector("[data-work-detail-close]")?.addEventListener("click",closeDetail);detailBody.querySelector("[data-work-follow]")?.addEventListener("click",e=>toggleFollow(e.currentTarget.dataset.workFollow));detailBody.querySelector('[data-work-action="start"]')?.addEventListener("click",startWork);detailBody.querySelector('[data-work-action="ai-review"]')?.addEventListener("click",aiReview);detailBody.querySelectorAll("[data-work-toggle]").forEach(b=>b.addEventListener("click",()=>setForm(b.dataset.workToggle,true)));detailBody.querySelectorAll("[data-work-form-cancel]").forEach(b=>b.addEventListener("click",()=>setForm(b.dataset.workFormCancel,false)));detailBody.querySelector("[data-work-evidence-form]")?.addEventListener("submit",e=>{e.preventDefault();addEvidence(e.currentTarget);});detailBody.querySelector("[data-work-complete-form]")?.addEventListener("submit",e=>{e.preventDefault();completeWork(e.currentTarget);});detailBody.querySelector("[data-work-approve-form]")?.addEventListener("submit",e=>{e.preventDefault();approveWork(e.currentTarget);});}
  if(!isOverview){
    search?.addEventListener("input",renderList);
    relationFilter?.addEventListener("change",()=>{filters.relation=relationFilter.value;renderList();});
    statusFilter?.addEventListener("change",()=>{filters.status=statusFilter.value;renderList();});
    priorityFilter?.addEventListener("change",()=>{filters.priority=priorityFilter.value;renderList();});
    businessFilter?.addEventListener("change",()=>{filters.business=businessFilter.value;renderList();});
    teamFilter?.addEventListener("change",()=>{filters.team=teamFilter.value;renderList();});
    projectFilter?.addEventListener("change",()=>{filters.project=projectFilter.value;renderList();});
    sortFilter?.addEventListener("change",()=>{filters.sort=sortFilter.value;renderList();});
    host.querySelector("[data-work-more-filter]")?.addEventListener("click",()=>{const advanced=host.querySelector("[data-work-advanced]");advanced.hidden=!advanced.hidden;});
    host.querySelector("[data-work-refresh]")?.addEventListener("click",refresh);
    host.querySelector(".miwa-work-view-toggle")?.addEventListener("click",e=>{const b=e.target.closest("[data-work-view]");if(!b)return;view=b.dataset.workView||"card";localStorage.setItem("aione.work.view.v1",view);renderList();});
    list?.addEventListener("click",e=>{const follow=e.target.closest("[data-work-follow]");if(follow){toggleFollow(follow.dataset.workFollow);return;}const b=e.target.closest("[data-work-open]");if(b)loadDetail(b.dataset.workOpen).catch(workError);});
    dialog?.addEventListener("close",()=>{activeDetail=null;publishWorkContext(null);});dialog?.addEventListener("click",e=>{if(e.target===dialog)closeDetail();});
  }
  await refresh();return true;
}
