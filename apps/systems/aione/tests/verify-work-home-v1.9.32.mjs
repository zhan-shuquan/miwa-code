import fs from "node:fs";import path from "node:path";import{fileURLToPath}from"node:url";const here=path.dirname(fileURLToPath(import.meta.url)),root=path.resolve(here,".."),read=p=>fs.readFileSync(path.join(root,p),"utf8"),must=(c,m)=>{if(!c)throw new Error(m)};const routes=read("js/config/route-registry.js"),sidebar=read("js/config/sidebar-registry.js"),system=read("js/miwa-system.js"),page=read("js/pages/miwa-work-home.js"),html=read("pages/work-home/template.html");for(const[id,label]of[["work-today","今日工作"],["work-mine","我的工作"],["work-all","全部工作"],["work-blocked","等待与阻塞"],["work-review","待验收"],["work-records","工作记录"]])must(routes.includes(`"${id}": route("${id}", "${label}"`),`缺少V2路由 ${id}`);must(sidebar.includes("route.parent === parentId && route.nav !== false"),"Sidebar未隐藏兼容路由");must(system.includes('"work-today","work-mine","work-all","work-blocked","work-review","work-records"'),"V2路由未进入独立初始化链");must(page.includes("美和工作9问")&&page.includes("后台复杂，前台简单"),"缺少V2方法论");must(page.includes("看得到 ≠ 要处理 ≠ 能操作"),"透明原则未进入界面");must(page.includes("/api/v1/work-home?limit=200")&&page.includes("/execution")&&page.includes("/start")&&page.includes("/evidence")&&page.includes("/complete")&&page.includes("/approve"),"既有工作事实/执行闭环被破坏");must(html.includes("miwa-work-manual"),"缺少出版物式活手册视觉层");console.log("V1.9.32 Work Home V2 first-upgrade validation passed.");
