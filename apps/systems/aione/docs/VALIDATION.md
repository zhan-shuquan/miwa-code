# V1.18.0 上架判断主流程与按需测样验收

1. 01选品工作台业务流程只显示5个固定节点：商品机会、数据录入、成本试算、智能定价、上架判断。
2. 页面、筛选与商品机会当前事项不得再把“测样”作为固定主流程节点。
3. 原“推进判断”前台名称统一为“上架判断”。
4. 成本与智能定价形成有效结果后，上架判断应可执行，不得因为未测样而默认禁用。
5. 测样模块继续保留，但标题明确为“测样验证（按需）”，默认状态为“按需 / 尚未启动”。
6. 测样可在上架判断前后进入；未完成测样不默认阻断上架判断。
7. 已完成测样的数据、异常、报告与实测重量继续回写选品记录，并作为上架判断的辅助证据。
8. 负面测样证据应形成风险提示并可改变系统建议，但最终业务结果仍只保留“上架 / 不上架”。
9. 上架判断为“上架”后，后续可继续编排视觉设计、必要采购、按需测样和上架发布准备；测样后补不应覆盖历史判断快照。
10. 全局Header与详情页嵌入Header显示“分析中心”，不得继续显示“管理驾驶舱”。
11. 业务类型保持3列：直发选品、常规选品、产品开发；类型标题使用更清晰的美和绿粗体表达。

---

# V1.17.0 选品概览／选品任务与业务类型入口验收

1. 选品工作台头部只保留「选品概览」「选品任务」两个入口，不显示「新建商品机会」「批量导入」「选品AI人才」。
2. 点击「选品概览」进入独立选品概览页面；点击「选品任务」进入独立选品任务页面；两页均可返回01选品工作台。
3. 业务类型只显示3列：直发选品、常规选品、产品开发；不得显示AI选品。
4. 每个业务类型卡片上段显示类型名称与现有数量；下段横向显示「新建商品机会｜批量导入」，中间有明确分隔。
5. 点击业务类型名称／数量继续筛选该类型现有商品机会。
6. 点击「新建商品机会」直接自动带入当前业务类型并进入数据录入，不再弹出二次类型选择。
7. 点击「批量导入」直接自动带入当前业务类型，批量导入弹窗不再要求选择类型，并自动匹配对应Excel模板。
8. 商品机会正式主流程保持：商品机会 → 数据录入 → 成本试算 → 智能定价 → 测样 → 推进判断。
9. 选品概览只统一选品思维与商品机会前置逻辑，不把未确认的真实方向、任务数量或经营数据伪造成正式数据。
10. 选品任务页当前保持真实空状态，待任务对象与持久化结构正式设计后再接入真实数据。

---

# V1.4.6 配送尺寸函数与代表图验收

1. 30×20×2.5cm 应推荐3cm薄型。
2. 40×15×2cm 虽厚度<=3cm但最长边>34cm，应推荐60サイズ。
3. 30×29×2.5cm 三边合计61.5cm，应推荐80サイズ。
4. 常规选品员工可在推荐档位基础上人工确认其他已启用配送档位；成本试算使用确认后的档位。
5. 日本配送费必须来自当前生效参数版本，不在尺寸函数中写死。
6. 直发选品尺寸仅保存真实数据，配送成本按销售套装预估毛重计算。
7. 代表图加载成功后空状态必须消失，图片完整contain显示。

---

# V1.4.5 核心录入最终收敛验收

1. “经营计算关键数据”小标题不再显示。
2. 商品名称下方直接进入四项核心经营事实。
3. 桌面端四项保持同一行：采购单价、销售套装数量、单件预估毛重、销售套装发货尺寸。
4. 前台单位显示为人民币 / 件 / 克 / 厘米。
5. 选品类型由商品机会继承，不在数据录入区重复选择。
6. 常规选品继续根据发货尺寸匹配配送档位并读取当前生效日本配送费参数。
7. 直发选品继续调用跨境直发成本模型。
8. 商品机会记录详情不得出现 CSV / Excel 导入导出入口。

---

# V1.4.4 发货尺寸与成本路由验收

1. 数据录入核心区桌面端显示四项经营关键数据：采购单价、销售套装数量、单件预估毛重、销售套装发货尺寸。
2. 前台单位显示中文：人民币 / 日元、件 / 套 / 双、克、厘米。
3. 直发选品自动使用跨境直发成本模型；常规选品自动使用日本仓发货成本模型。
4. 常规选品录入长宽高后，系统自动形成配送档位和日本配送费；费用来自当前生效“日本配送费标准”参数。
5. 发货尺寸保存草稿后刷新，应按 opportunity_id 自动恢复。
6. AI选品和产品开发本轮不自动猜履约路由。
7. 01选品工作台、标准业务流程、Header / Sidebar / Aside / Footer、测样与推进判断结构不得变化。

---

# V1.4.2 草稿保存与自动恢复验收

1. 在同一商品机会填写任意部分字段后，“录入状态”应显示“有未保存修改”。
2. 点击“保存草稿”时，不要求代表图和核心字段全部完成。
3. 保存成功后状态显示“草稿已保存｜HH:MM”。
4. 刷新或重新进入同一个 `opportunity_id` 后，普通字段应自动恢复。
5. 图片链接代表图应随草稿自动恢复。
6. 本地上传代表图应通过 IndexedDB 自动恢复。
7. 不同 `opportunity_id` 的草稿不得相互覆盖。
8. “提交选品资料”仍必须校验代表图和原有 required 字段。
9. 草稿存储仅用于本地原型验证，不视为正式业务数据库。

---

# V1.4.1 图片链接输入框验收

1. “粘贴图片链接”必须显示为可输入 URL 的输入框，而不是按钮或 prompt。
2. 粘贴有效图片直链后应自动尝试显示代表图预览。
3. 输入后按 Enter 或使输入框产生 change 时也应尝试预览。
4. 图片加载成功后 `data-image-confirmed` 才能变为 `true`。
5. 无效 URL / 无法直接访问的图片不得误判为已确认，并应提示用户检查链接。
6. 上传本地图片与“从采购来源提取”保持原职责和结构。
7. 数据录入其他核心字段、业务流程以及01选品工作台不得因本次修复发生变化。

---

# V1.4 数据录入模块验收

1. 头部只显示“数据录入”，第二行顺序固定为：选品负责人 → 录入时间 → 选品编号 → 正式货号。
2. 选品代表图标题旁保留帮助；代表图必须最终形成并确认。
3. 图片人工入口固定为“上传本地图片 / 粘贴图片链接”。
4. “从采购来源提取”单独作为AI能力入口，采购链接为空时不可执行。
5. 右侧核心录入只突出：采购来源链接、商品名称、采购单价、销售套装数量、单件预估毛重。
6. 采购单价 / 销售套装数量 / 单件预估毛重在桌面端同一行。
7. 来源平台 / 采购来源 / 商品分类 / 销售套装预估毛重只读自动形成，不要求员工填写。
8. 销售套装预估毛重继续由单件毛重 × 套装数量自动计算。
9. 直发选品可按已知规则自动带入跨境直发；其他类型不猜履约方式。
10. 当前静态版本不得伪造AI提取结果或正式草稿持久化。
11. 01选品工作台、标准业务流程、Shell公共组件、成本试算/智能定价/测样/推进判断结构不得因本次修改丢失。

---

# V1.3 标准业务流程模块验收

1. 业务流程模块默认仅三行：标题与上下文、简短说明、箭头流程。
2. 图标表达闭环业务流程，不使用左右往返语义。
3. 六个流程节点必须等宽铺满可用宽度，并保持明显单向箭头。
4. 节点内容不得贴近承接缺口或箭头尖端。
5. 顺序编号统一 01 / 02 / 03 / 04 / 05 / 06。
6. 已完成状态统一为美和绿圆形 + 白色 ✓。
7. 当前状态统一为美和绿实心圆；待进入统一为浅色圆。
8. 原有流程状态动态更新必须继续有效。
9. 01选品工作台与详情页其他业务区不得因本次替换改变。

---

# V1.2 头部与返回定位验收

1. 商品机会记录详情主标题上方不得再出现独立返回行。
2. 头部右侧应显示唯一页面级按钮「← 返回商品机会一览」。
3. 点击该按钮应返回 01 选品工作台，并自动定位到「商品机会一览」区块。
4. 页面底部如保留返回操作，名称同样统一为「返回商品机会一览」。
5. 点击 Sidebar「选品工作台」仍应返回一级页面顶部，不强制定位一览区。
6. 01选品工作台的头部、业务类型、业务流程、业务指标、商品机会一览及后续辅助区不得被删改。
7. 商品机会记录详情的六节点流程及后续业务区不得因本次头部调整发生结构变化。

---

# 验证记录

日期：2026-08-12

当前结果：代码结构验证通过，视觉效果待用户本地预览确认。

已完成：

- 全部 JavaScript 文件语法检查通过。
- 正式加载区 CSS 大括号与文件结构检查通过。
- CSS 总入口的全部 `@import` 路径存在。
- JS 配置中的全部组件路径存在。
- HTML 引用的 Logo、CSS、JS 路径存在。
- 使用本地 HTTP 服务验证主要入口、公共 CSS、公共 JS、Header、Sidebar、Aside、Footer 和 Logo 均返回 200。
- 旧混合 CSS 仅保存在 `recovery/`，未被正式入口加载。

尚待完成：

- 在用户 VS Code／Live Server 中确认桌面、平板、手机视觉效果。
- 用户确认后决定是否将候选版提升为正式锁定基线。
- 后续取得最新选品工作台与选品记录真实代码后，执行页面与模块接入验证。

## 2026-08-14｜01 选品工作台锁定前验收项

已完成代码级检查：

- `js/pages/selection-workbench.js` 语法检查通过。
- `js/pages/selection-record-preview.js` 语法检查通过。
- 列表／卡片互斥规则已加入 `[hidden]{display:none !important}` 防回归控制。
- 四种“新建商品机会”选品类型均绑定到选品录入路由验证页，并自动带入选品类型。
- 卡片“判断依据”默认折叠；异常条件使用 `open` 自动展开。
- 页面概览模块与商品机会模块已分成独立视觉容器。
- 帮助图标统一使用轻量 SVG 图标。

用户本地预览需重点确认：

1. 点击“＋ 新建商品机会”后四种类型卡片的大小、说明和动效是否舒适；
2. 点击任一类型是否进入选品录入页，并正确显示所选类型；
3. 点击“列表／卡片”是否只显示一种视图；
4. 普通卡片“判断依据”是否默认收起，异常卡片是否自动展开；
5. 卡片底部是否为“要素 9/9｜结果｜编辑”的平衡结构；
6. 页面概览与商品机会是否具有明确模块边界；
7. 业务类型、业务流程、业务指标旁的帮助图标是否自然。

## 2026-08-14｜V1.0 锁定最终验证

- 所有 JavaScript 文件通过 `node --check`。
- 所有 HTML 文件未发现重复 ID。
- 所有 CSS 文件通过语法解析检查。
- 列表与卡片继续共用 `js/data/selection-opportunities.js`。
- AI人才卡片继续通过 `ai_talent_id=selection_ai` 调用 `js/data/ai-talents.js`。
- AI人才头像接口已支持未来由AI人才库动态提供图片。
- 商品机会卡片已整合最终锁定文案与结果布局。

## V1.0.1 导航专项验收

1. 打开根目录 `index.html`，01选品工作台完整V1.0页面必须原样显示。
2. 卡片/列表后半部分、业务关键要素区、AI人才等原锁定模块不得丢失。
3. 点击 `XP20260811000002` 的“编辑”进入 `pages/selection-workbench/record-detail/index.html`；其他记录暂不进入该固定恢复详情模板。
4. 详情页地址必须携带 `opportunity_id`，且 Hash 表达 `/selection/opportunity/:id`。
5. 点击“返回商品机会列表”返回根目录 `index.html#/selection`。
6. 点击详情页底部“返回记录列表”返回根目录 `index.html#/selection`。
7. 详情页左侧“选品工作台”返回根目录一级页面，不停留在详情HTML。
8. 点击“提交选品资料”不得离开当前详情HTML，应定位到成本试算。
9. 详情页Main顶部不显示“完整业务恢复候选预览 / CANDIDATE”等开发说明。
10. 本轮不以二级详情替换一级页面，也不以一级页面简化二级详情。


# V1.1 统一入口验收

1. 01选品工作台完整页面与V1最终锁定版视觉和区块保持一致。
2. 任意商品机会卡片/列表点击「编辑」均进入商品机会记录详情，不再加载旧选品录入页。
3. 详情页显示的 XP 编号、商品名称、选品类型、负责人、选品时间应与被点击商品机会一致。
4. 「＋新建商品机会」→选择类型→系统生成新的 XP 编号→进入同一商品机会记录详情；商品名称等未形成事实保持空白/待确认。
5. 详情页「← 返回商品机会列表」「返回记录列表」和 Sidebar「选品工作台」均返回完整01选品工作台。
6. 正式导航代码中不得再引用 `record-preview.html` 或 `initSelectionRecordPreview`。
7. 旧选品录入相关文件允许保留，但不得被正式导航调用。
# V1.14.1 来源链接与代表图交互验收

1. 采购来源输入有效 HTTP/HTTPS URL 后，必须显示“打开采购来源 ↗”；链接为空或 URL 无效时入口隐藏。
2. “打开采购来源 ↗”必须在新标签页打开原始采购来源，不改变输入框内容。
3. 代表图常态只显示“上传本地图 / 填写图片链接 / 查看大图”，不得长期裸露技术型图片 URL。
4. 点击“填写图片链接”后才展开 URL 输入与保存按钮；有效图片加载成功后收起编辑器并显示“图片链接｜已保存”。
5. 无效 URL 或无法加载的图片不得被确认成代表图。
6. 图片确认后，“查看大图”和点击代表图本身均可打开灯箱。
7. 灯箱必须支持放大、缩小、适应屏幕、打开原图、关闭按钮和 Esc 关闭。
8. 本地上传图片仍可查看大图，草稿保存与恢复逻辑不受影响。
9. 不得修改已锁定的数据录入、成本试算、智能定价、测样和推进判断业务结构。

