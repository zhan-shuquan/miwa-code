> V1.18.0 CANDIDATE：正式主流程收敛为“商品机会 → 数据录入 → 成本试算 → 智能定价 → 上架判断”；测样改为按需辅助验证，可在上架判断前后触发；全局“管理驾驶舱”更新为“分析中心”。

> V1.17.0.1 CANDIDATE：修复业务类型容器布局，桌面端恢复直发选品／常规选品／产品开发3列显示；不改变V1.17.0业务逻辑。

> V1.17.0 CANDIDATE：新增选品概览／选品任务独立入口；业务类型收敛为直发、常规、产品开发三类，并在卡片内直接新建／批量导入。商品机会后的正式主流程保持不变。


## V1.16.4_CANDIDATE 更新说明

运营模拟按真实经营阶段加载默认值：观察期为“广告0% / 优惠券0% / 积分1% / 其他0 JPY”；运营中读取全局当前生效运营参数。楽天基础1倍积分继续归属于运营投入成本，并在观察期真实成本中按1%计入。

> V1.16.3：系统参数已迁移至 Header 全局“设置 → 系统参数”，业务工作台只读当前生效参数版本。
# V1.14.1｜来源链接与代表图交互补丁

当前版本在 V1.14.0 业务结构锁定基线上，仅补齐两个真实使用缺口：采购来源可直接打开、代表图图片链接按需编辑并支持大图灯箱查看。其他业务结构与规则保持冻结。

# 美和 AIONE｜01 选品工作台首页 V1.0 正式锁定版

状态：**01 选品工作台首页 V1.0 正式锁定基线**。后续修改必须进入正式变更流程。

> 说明：选品录入页当前仍为业务链路验证接入口，不冒充此前尚未正式嫁接的最终选品录入页面。

本版本以当前 AIONE System Shell 为唯一基线，不重构 Header、Primary Navigation、Aside、Footer，只完成 01 选品工作台 Main、选品录入接入口、AI人才卡片及其数据层的统一收口。

## 本次最终收口

- 列表：实务化简化显示，12项／页，与卡片同页同数据，结果始终可见。
- AI人才：三区母版＋`ai_talent_id` 数据驱动，身份与头像可由未来AI人才库动态更新。
- 商品机会卡片：采用最终锁定母版，目标文案、结果胶囊与判断依据表达统一。

## 当前完成范围

### 01 选品工作台首页

- 页头：`＋ 新建商品机会`、`选品AI人才`
- 页面概览模块：业务类型、业务流程、业务指标
- 商品机会模块：搜索筛选、列表／卡片切换、同一批12个商品机会
- 商品机会卡片：代表图、名称、编号、选品类型、采购来源、负责人、当前事项、平台、时间、投入成本、信息、判断依据、要素、结果、编辑
- 列表视图：`商品信息摘要｜负责人｜当前事项｜平台｜时间｜投入成本｜信息｜结果`
- 当前投入成本：按当前筛选结果由确定性函数实时汇总
- 业务关键要素：页面下部保留完整 9/9 说明

### 选品录入

业务链路：

`＋ 新建商品机会 → 选择选品类型 → 系统生成商品机会ID → 进入数据录入 → 自动带入选品类型`

已有商品机会点击“编辑”时进入同一录入页面，并带入已有标题、负责人、来源平台等可用数据。

### 选品AI人才卡片

卡片采用三段式母版：

1. 头部：AI身份与成熟度
2. 中部：业务流程能力
3. 尾部：实战表现

页面只保存 `ai_talent_id="selection_ai"`。AI人才名称、类型、职责、流程覆盖、节点经验与整体实战数据来自 `js/data/ai-talents.js`。

未来将该数据层替换为正式 AI人才库数据库/API 时，卡片本体不需要重写。

没有真实验证证据的数据统一显示“待验证”，不使用示例数字冒充正式成果。

## 关键文件

- 公共 CSS 总入口：`css/miwa-system.css`
- 公共 JS 总入口：`js/miwa-system.js`
- 系统动态配置：`js/config/system-config.js`
- 选品工作台页面：`pages/selection-workbench/home.html`
- 选品工作台样式：`css/pages/selection-workbench.css`
- 选品工作台交互：`js/pages/selection-workbench.js`
- 选品录入页面：`pages/selection-workbench/record-preview.html`
- 选品录入交互：`js/pages/selection-record-preview.js`
- 12个商品机会统一数据源：`js/data/selection-opportunities.js`
- AI人才库本地数据层：`js/data/ai-talents.js`
- AI人才卡片母版：`components/ai/ai-talent-card.html`
- AI人才卡片样式：`css/components/ai-talent-card.css`
- AI人才卡片逻辑：`js/components/ai-talent-card.js`

## 本地预览

项目使用 `fetch()` 加载独立 HTML 组件，建议在 VS Code 中使用 Live Server 打开项目根目录。

也可在项目根目录运行：

```text
python -m http.server 8080
```

再打开：

```text
http://localhost:8080
```

## 本轮验收路径

建议严格按以下顺序验证：

1. 首页完整显示 Header / Sidebar / Main / Aside / Footer；
2. 业务流程第06项“推进判断”完整显示；
3. 列表与卡片可以互斥切换；
4. 两种视图保持同一批12项数据；
5. 列表8列在 Main 内完整显示，结果列无需横向滚动；
6. “商品信息摘要”显示代表图、商品名、XP编号、选品类型、采购来源与“要素 x/9”入口；
7. “当前事项”显示当前节点与编辑入口；
8. 平台超出时使用 `+N` 收敛，不破坏列表；
9. 当前投入成本随筛选结果变化；
10. 新建商品机会选择类型后进入选品录入，并自动带入类型和商品机会ID；
11. 点击“选品AI人才”打开三段式AI人才卡片；
12. AI人才卡片身份信息来自 `ai_talent_id` 对应的数据对象。

## 保护规则

1. Header、Primary Navigation、Aside、Footer 属于公共 Shell，不由单个页面改写。
2. 列表与卡片必须共用同一数据源，不能各维护一套数据。
3. AI人才卡片不写死人才身份数据，只通过 `ai_talent_id` 调用人才库。
4. AI能力、覆盖级、实战次数、独立处理率等没有真实证据时必须显示“待验证”。
5. 本版本已正式锁定；后续不再做无价值像素级微调，如需修改必须明确记录变更原因与影响范围。


## V1.0 列表锁定说明
列表前台采用实务化简化表达，不机械展示九要素名称。商品信息摘要承担商品识别职责，集中展示代表图、商品名、XP编号、选品类型与采购来源。九要素继续作为底层业务模型和页面下方“业务关键要素”解释体系存在。

采购来源链接通过数据字段 `sourceUrl` 提供。只有真实链接存在时前台显示可点击来源链接；当前原型演示数据未提供经核实的真实采购 URL，因此不生成虚假链接。

## V1.0 最终锁定补充｜列表与卡片同源表达

- 卡片与列表继续共用 `js/data/selection-opportunities.js`，不分别维护业务数据。
- 列表采用更适合批量扫读的8列布局，但核心内容与卡片保持一致。
- “商品信息摘要”集中显示：选品代表图、商品名称、商品机会编号、选品类型、采购来源、`要素 x/9`。
- `要素 x/9` 与卡片中的“要素”入口调用同一业务关键要素说明层。
- 采购来源仅在 `sourceUrl` 存在真实链接时显示可点击 `来源 ↗`；无真实 URL 时只显示来源平台名称，不伪造链接。
- 负责人、当前事项、销售平台、时间、投入成本、信息状态、结果与卡片读取同一字段，保证两种视图语义一致。

> 锁定原则：**卡片和列表同源数据、同一业务含义、不同视觉表达。**

## V1.0.1｜商品机会记录详情导航接入

本版以 `20260814_24_MIWA_AIONE_SELECTION_WORKBENCH_V1_FINAL_LOCKED.zip` 为一级页面唯一基线，并把 `20260814_31_AIONE_SELECTION_RECORD_RESTORED_PREVIEW_V0_4_1_HEADER_BASELINE.zip` 作为独立二级页面接入。

页面边界：

- `index.html#/selection`：01 选品工作台一级页面，原锁定内容保持不变。
- `pages/selection-workbench/record-detail/index.html?opportunity_id=XP...#/selection/opportunity/XP...`：商品机会记录详情二级页面。
- 当前已恢复并验证的商品机会 `XP20260811000002` 点击“编辑”进入二级详情；其他商品机会暂时保留V1.0原有录入预览链路，避免使用固定恢复数据冒充其他记录。
- 二级详情的“返回商品机会列表”与“返回记录列表”返回一级选品工作台。
- 二级详情 Sidebar 点击“选品工作台”同样返回一级选品工作台。
- “＋新建商品机会”继续使用V1.0原有新建/数据录入链路，不与当前详情恢复模板混用。
- Main 顶部开发/恢复说明从详情业务页面移除，开发信息保留在文档。

锁定保护：本次不修改 01选品工作台 `home.html`、卡片/列表结构、概览、业务关键要素、AI人才卡片、Header、Sidebar、Aside、Footer 的内容与视觉。

## V1.1｜商品机会详情统一入口

- 「＋新建商品机会」与商品机会「编辑」统一进入 `pages/selection-workbench/record-detail/index.html`。
- 原 `record-preview.html｜选品录入` 退出正式导航链路，当前仅保留为历史恢复参考，不删除。
- 数据录入不再作为独立页面；它是「商品机会记录详情」中的业务节点。
- 编辑现有记录时，详情页从 `js/data/selection-opportunities.js` 读取同一商品机会的 ID、名称、选品类型、负责人、时间与来源。
- 新建商品机会时，系统先生成 XP 编号并带入选品类型，再进入同一详情页的数据录入节点；静态预览不伪造尚未录入的商品名称、来源和其他事实。
- 01选品工作台最终锁定页面结构、商品机会卡片、页面后半部分与全局 Shell 保持不变。


## V1.2｜商品机会记录详情标准头部与返回一览

- 二级页面头部继续沿用统一母版：左侧固定身份区，右侧页面级操作区。
- 原主标题上方独立「返回商品机会一览」已删除，不再形成额外视觉层级。
- 「返回商品机会一览」移动到头部右侧，作为页面级导航操作。
- 页面内相关名称统一为「商品机会一览」，废止「商品机会列表 / 返回记录列表」等近义表达。
- 返回动作精准定位到 01 选品工作台的「商品机会一览」区块，而不是工作台顶部。
- Sidebar「选品工作台」仍按工作台入口职责返回一级页面顶部，与“返回商品机会一览”职责分离。
- 六节点流程、商品机会详情业务内容、01选品工作台页面结构均未修改。


## V1.3｜标准业务流程模块合并

- 商品机会记录详情原圆点进度条替换为美和标准业务流程三行母版。
- 第一行：闭环流程图标 +「业务流程」+ 帮助 + 当前节点。
- 第二行：一句简短业务说明。
- 第三行：连续箭头节点，六节点等宽铺满。
- 节点编号统一 01 / 02 / 03…；已完成统一美和绿圆形白色 ✓；当前为美和绿实心状态圆；待进入为浅色状态圆。
- 节点内容向内部收紧，避免文字和状态信息贴近箭头尖端或承接缺口。
- 本次不修改 01选品工作台、页面头部、成本试算、智能定价、测样和推进判断业务区。

## V1.4｜数据录入模块最小人工录入 + AI-ready

- 数据录入头部锁定为两行：标题；选品负责人 → 录入时间 → 选品编号 → 正式货号。
- 选品代表图保持必需：人工可上传本地图片或粘贴图片链接；“从采购来源提取”作为独立AI能力入口。
- 核心录入正式收敛为：一个采购来源链接 + 商品名称 + 采购单价 + 销售套装数量 + 单件预估毛重。
- 采购单价、销售套装数量、单件预估毛重统一同一行。
- 来源平台、采购来源、商品分类、销售套装预估毛重进入只读“系统自动形成”区域，不要求员工重复填写。
- 直发选品的履约方式按已知业务规则自动带入“跨境直发”；其他类型不猜。
- 当前静态版本不伪造AI结果：AI提取/自动生成尚未接入时给出明确状态提示。
- 页面提交时，选品代表图和五项核心录入信息均作为当前数据录入必要条件。


## V1.4.1｜选品代表图图片链接输入修复

- “粘贴图片链接”由普通按钮改为可直接输入/粘贴 URL 的输入框。
- 粘贴、按 Enter 或离开输入框后均可触发代表图预览。
- 图片实际加载成功后才标记代表图已确认；链接无效或图片不可访问时保持未确认并提示。
- “上传本地图片”和“从采购来源提取”职责保持不变。
- 本次不修改数据录入其他字段、标准业务流程及其他工作台。

## V1.4.2｜商品机会草稿本地持久化

- 数据录入草稿按 `opportunity_id / 选品编号` 独立保存。
- 普通字段与图片链接保存到 LocalStorage。
- 本地上传的选品代表图保存到 IndexedDB。
- 点击“保存草稿”允许资料不完整，不触发提交校验。
- 再次打开同一个商品机会时自动恢复上次草稿。
- 尾部状态显示“有未保存修改 / 草稿已保存｜时间 / 草稿已恢复｜最后保存时间”。
- “提交选品资料”仍保留代表图与核心必填字段校验。
- 当前方案仅用于本地原型真实业务验证，不替代未来正式数据库。

## V1.4.3｜数据录入核心区视觉锁定微调

- 商品名称改为与采购来源链接、系统自动形成一致的独立完整信息框。
- 采购来源链接与商品名称之间增加纵向间距，明确“来源 → 名称 → 经营计算数据”的阅读层级。
- 不改变草稿保存、自动恢复、AI状态、经营计算字段和后续业务节点逻辑。
- 本轮通过后，数据录入核心区视觉结构作为当前锁定基线。

## V1.4.4｜发货尺寸、选品类型路由与中文单位

- 数据录入核心区新增“销售套装发货尺寸（长×宽×高）”，单位前台统一显示“厘米”。
- 数据录入单位前台统一中文显示：人民币 / 日元、件 / 套 / 双、克、厘米；底层值继续保留 CNY / JPY 等标准代码。
- 选品类型由商品机会自动继承，不要求员工重复选择：直发选品→跨境直发成本模型；常规选品→日本仓发货成本模型；AI选品 / 产品开发暂不猜路由。
- 系统自动形成区新增：选品类型、配送档位、日本配送费。
- 日本配送费继续读取现有“日本配送费标准”当前生效参数。当前首版匹配规则按现有档位名称执行：高≤3cm优先3cm薄型，否则按长+宽+高匹配60 / 80 / 100 / 120サイズ。
- 发货尺寸已纳入 LocalStorage 草稿保存与自动恢复。
- 其他已锁定模块未修改。

## V1.4.5｜核心录入最终收敛

- 删除“经营计算关键数据”小标题与说明，核心字段直接承接商品名称，减少重复层级。
- 核心录入保持：采购来源链接 → 商品名称 → 采购单价 / 销售套装数量 / 单件预估毛重 / 销售套装发货尺寸。
- 前台单位继续统一中文显示：人民币 / 件 / 克 / 厘米；底层继续使用标准代码。
- 选品类型继续由商品机会自动继承，并作为成本试算路由条件，不要求员工重复选择。
- 系统自动形成继续只读显示：来源平台、采购来源、选品类型、商品分类、销售套装预估毛重、配送档位、日本配送费。
- 日本配送费继续读取现有“日本配送费标准”当前生效参数。
- 商品机会记录详情不加入 CSV / Excel 导入导出；批量导入导出留给需要批量数据处理的一级页面/列表模块。


## V1.4.6｜配送尺寸函数与代表图显示修复

- 销售套装发货尺寸独立成一行，员工录入真实长、宽、厚度（高）。
- 常规选品使用确定性函数：3cm薄型必须同时满足厚度<=3cm、最长边<=34cm、三边合计<=60cm；否则按三边合计匹配60/80/100/120サイズ。
- 系统显示最长边、三边合计和推荐配送档位；员工通过“确认配送档位”下拉框进行最终确认。
- 日本配送费不写死在函数中，读取当前生效参数版本；默认建议值同步为200/500/600/700/800 JPY。
- 直发选品保留长宽厚真实数据，但运费继续按销售套装预估毛重调用跨境直发参数。
- 选品代表图成功加载后强制隐藏空状态，图片使用contain完整显示，不裁切、不拉伸。

## V1.4.8｜尺寸单行最终收敛

- V1.4.7 候选不采用，本版重新以 V1.4.6 为正确业务基线。
- 采购单价、销售套装数量、单件预估毛重同一行、独立框。
- 销售套装发货尺寸与确认配送档位在桌面端固定同一行。
- 人工区只保留长、宽、厚度（高）、确认配送档位。
- 最长边、三边合计、系统推荐配送档位、日本配送费统一移入“系统自动形成”。
- 原尺寸函数、参数读取、直发选品重量运费、草稿保存与代表图完整显示全部保留。

## Preview登录与权限验证｜V1.18.1 CANDIDATE

本版本在V1.18.0真实代码基线上加入阶段3前置的Preview身份与权限验证能力。

### 本地验证

启动静态HTTP服务后访问根目录。首次进入会显示AIONE Preview身份选择页，可切换7名现有成员与1个公司管理身份，验证Header用户信息、权限入口和店铺范围配置。

### 重要安全说明

当前身份选择器仅用于本地预演，不等同于Google登录。正式Preview部署必须使用Google OIDC/受控登录，并在服务端完成白名单、身份映射、权限、数据范围、API授权和审计日志校验。
