# 恢复证据区

本目录保存上传恢复包中的原始混合 CSS 与来源说明，仅用于追溯和后续人工比对。

重要发现：原 `components.css` 和 `responsive.css` 同时混入了公共框架、选品工作台、商品机会、编辑表单、原型验证等多类样式。由于对应 HTML/JS 不完整，且无法确认是否为后来锁定的最终版本，这些文件不参与新结构运行加载。

它们没有被删除，只是与正式代码隔离。
